---
phase: 16-documentation-corrections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - SETUP.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
  - DOCS-03

must_haves:
  truths:
    - "README Docker Services table shows `lpg-db` (not `routing-db`) with correct health check"
    - "README Environment Variables table shows `routing_opt` as POSTGRES_DB default (not `routeopt`)"
    - "README Quick Start annotates steps that are automated by init containers / start.sh"
    - "README and SETUP.md `<REPO_URL>` placeholders have developer notes about replacement before delivery"
    - "SETUP.md Postgres defaults match .env.example (`routing` user, `routing_opt` db)"
  artifacts:
    - path: "README.md"
      provides: "Corrected container names, credential defaults, automated step annotations, REPO_URL note"
      contains: "lpg-db"
    - path: "SETUP.md"
      provides: "Corrected Postgres defaults, REPO_URL note"
      contains: "routing_opt"
  key_links:
    - from: "README.md"
      to: "docker-compose.yml"
      via: "Docker Services table references"
      pattern: "lpg-db.*pg_isready.*routing"
    - from: "SETUP.md"
      to: ".env.example"
      via: "Environment variable defaults"
      pattern: "routing_opt"
---

<objective>
Fix factual inaccuracies in README.md and SETUP.md: stale container names, wrong credential defaults, missing REPO_URL developer notes, and unannotated automated steps.

Purpose: README and SETUP.md are developer-facing docs that must match the actual docker-compose.yml, .env.example, and scripts. Stale values cause confusion when developers try to debug or extend the system.

Output: Corrected README.md and SETUP.md with all factual claims verified against source-of-truth files.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-documentation-corrections/16-CONTEXT.md
@.planning/phases/16-documentation-corrections/16-RESEARCH.md

@README.md
@SETUP.md
@docker-compose.yml
@.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix README.md factual inaccuracies</name>
  <files>README.md</files>
  <action>
Apply all corrections below. Cross-reference each change against docker-compose.yml and .env.example to confirm accuracy before writing.

**Line 15 (REPO_URL):** Add a developer-facing comment after or below the git clone line:
```
git clone <REPO_URL> routing_opt && cd routing_opt
# ^^^ Replace <REPO_URL> with the actual repository URL before customer delivery
```

**Lines 32-34 (manual health check):** Add annotation comment:
```
# start.sh handles health polling -- only needed if running outside Docker
sleep 5
curl http://localhost:8000/health   # should return {"status":"ok"}
docker compose ps                   # all services should show "running"
```

**Line 37 (alembic upgrade head):** Add annotation comment:
```
# Automated by db-init container -- only needed if running outside Docker
alembic upgrade head
```

**Line 404 (Docker Services table row for PostgreSQL):** Change from:
```
| PostgreSQL + PostGIS | `routing-db` | 5432 | `pg_isready -U routeopt` |
```
To:
```
| PostgreSQL + PostGIS | `lpg-db` | 5432 | `pg_isready -U routing -d routing_opt` |
```

**Line 409 (OSRM note):** Update to reflect automatic handling:
```
> **Note:** OSRM requires preprocessed Kerala map data in `data/osrm/`. This is downloaded automatically on first `docker compose up`. See [SETUP.md](SETUP.md) for manual download steps if running OSRM outside Docker.
```

**Line 426 (Environment Variables table, POSTGRES_DB row):** Change from:
```
| `POSTGRES_DB` | No | Defaults to `routeopt` |
```
To:
```
| `POSTGRES_DB` | No | Defaults to `routing_opt` |
```

**Verify each change** against docker-compose.yml before saving:
- Container name: `lpg-db` (docker-compose.yml `container_name` field)
- Health check: `pg_isready -U routing -d routing_opt` (docker-compose.yml `healthcheck` block)
- POSTGRES_DB default: `routing_opt` (.env.example)
  </action>
  <verify>
    <automated>cd /home/vishnu/projects/routing_opt && grep -c 'routing-db' README.md | grep '^0$' && grep 'lpg-db' README.md && grep 'routing_opt' README.md | grep -q 'POSTGRES_DB' && grep -q 'REPO_URL' README.md && grep -q 'only needed if running outside Docker' README.md && echo "PASS: All README corrections verified"</automated>
  </verify>
  <done>
- `routing-db` no longer appears in README.md (0 occurrences)
- `lpg-db` appears in Docker Services table
- `pg_isready -U routing -d routing_opt` is the health check command
- POSTGRES_DB defaults to `routing_opt` (not `routeopt`)
- REPO_URL has developer note about replacement
- Lines 32-37 annotated as automated / only needed outside Docker
- OSRM note updated to mention automatic download
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix SETUP.md factual inaccuracies</name>
  <files>SETUP.md</files>
  <action>
Apply corrections below. Cross-reference each change against .env.example.

**Line 49 (REPO_URL):** Add developer note below the git clone line, matching README pattern:
```
git clone <REPO_URL> routing_opt
# ^^^ Replace <REPO_URL> with the actual repository URL before customer delivery
cd routing_opt
```

**Line 179 (POSTGRES_USER default):** Change from:
```
| `POSTGRES_USER` | Defaults to `routeopt` | No |
```
To:
```
| `POSTGRES_USER` | Defaults to `routing` | No |
```

**Line 180 (POSTGRES_DB default):** Change from:
```
| `POSTGRES_DB` | Defaults to `routeopt` | No |
```
To:
```
| `POSTGRES_DB` | Defaults to `routing_opt` | No |
```

**Verify each change** against .env.example before saving:
- POSTGRES_USER: `routing` (.env.example line 18)
- POSTGRES_DB: `routing_opt` (.env.example line 20)
  </action>
  <verify>
    <automated>cd /home/vishnu/projects/routing_opt && grep -c 'routeopt' SETUP.md | grep '^0$' && grep -q 'REPO_URL' SETUP.md && grep 'routing_opt' SETUP.md | grep -q 'POSTGRES_DB' && grep 'routing' SETUP.md | grep -q 'POSTGRES_USER' && echo "PASS: All SETUP.md corrections verified"</automated>
  </verify>
  <done>
- `routeopt` no longer appears in SETUP.md (0 occurrences)
- POSTGRES_USER defaults to `routing`
- POSTGRES_DB defaults to `routing_opt`
- REPO_URL has developer note about replacement
  </done>
</task>

</tasks>

<verification>
Run the full grep verification suite to confirm no stale values remain:

```bash
# Should return 0 for all stale patterns
grep -c 'routing-db' README.md         # expect: 0
grep -c 'routeopt' README.md           # expect: 0
grep -c 'routeopt' SETUP.md            # expect: 0

# Should find corrected values
grep 'lpg-db' README.md                # expect: match in Docker Services table
grep 'routing_opt' README.md           # expect: match in Environment Variables table
grep 'routing_opt' SETUP.md            # expect: match in Environment Variables table

# REPO_URL should have developer notes nearby
grep -A1 'REPO_URL' README.md          # expect: note about replacement
grep -A1 'REPO_URL' SETUP.md           # expect: note about replacement
```
</verification>

<success_criteria>
1. Zero occurrences of `routing-db` in README.md
2. Zero occurrences of `routeopt` in README.md and SETUP.md
3. `lpg-db` appears in README Docker Services table with correct health check
4. POSTGRES_DB defaults to `routing_opt` in both README and SETUP.md
5. REPO_URL placeholders have developer notes in both files
6. Automated steps (alembic, health polling) annotated in README Quick Start
</success_criteria>

<output>
After completion, create `.planning/phases/16-documentation-corrections/16-01-SUMMARY.md`
</output>
