---
phase: 20-sync-error-message-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CSV_FORMAT.md
  - DEPLOY.md
  - .planning/phases/20-sync-error-message-documentation/ERROR-MAP.md
autonomous: true
requirements: [CSV-04, ERR-01, ERR-02]

must_haves:
  truths:
    - "Every error message string in CSV_FORMAT.md 'What You See' column matches the exact code output verbatim"
    - "DEPLOY.md troubleshooting code blocks show dashboard messages, not Python exception types or set notation"
    - "CSV_FORMAT.md geocoding table has 7 entries (6 named + 1 fallback), all matching GEOCODING_REASON_MAP + fallback code"
    - "CSV_FORMAT.md row-level table has 9 entries (3 pre-validation + 6 _humanize_row_error patterns)"
    - "DEPLOY.md 'File not found' section is removed"
    - "DEPLOY.md contains cross-link to CSV_FORMAT.md for complete error list"
    - "ERROR-MAP.md traces every documented message to a code file:line"
  artifacts:
    - path: "CSV_FORMAT.md"
      provides: "Updated error message tables with verbatim code strings"
      contains: "Address not found -- check spelling in CDCMS"
    - path: "DEPLOY.md"
      provides: "Updated troubleshooting with humanized messages"
      contains: "CSV_FORMAT.md > What Can Go Wrong"
    - path: ".planning/phases/20-sync-error-message-documentation/ERROR-MAP.md"
      provides: "Error message traceability artifact"
      contains: "verified"
  key_links:
    - from: "CSV_FORMAT.md"
      to: "apps/kerala_delivery/api/main.py"
      via: "GEOCODING_REASON_MAP values verbatim in geocoding table"
      pattern: "Address not found -- check spelling"
    - from: "CSV_FORMAT.md"
      to: "core/data_import/csv_importer.py"
      via: "_humanize_row_error patterns verbatim in row-level table"
      pattern: "Invalid number value -- check for letters"
    - from: "DEPLOY.md"
      to: "core/data_import/cdcms_preprocessor.py"
      via: "Required columns missing message in troubleshooting"
      pattern: "Required columns missing"
---

<objective>
Sync all error messages in CSV_FORMAT.md and DEPLOY.md to match the actual humanized output produced by Phase 17 code changes. Fix 8 stale messages, add 7 missing entries, remove 1 obsolete section, and create a traceability artifact.

Purpose: Office employees match what they see on screen to the documentation. Stale messages cause confusion -- the employee sees one thing but the doc says something different.
Output: Updated CSV_FORMAT.md, updated DEPLOY.md, new ERROR-MAP.md verification artifact
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-sync-error-message-documentation/20-CONTEXT.md
@.planning/phases/20-sync-error-message-documentation/20-RESEARCH.md
@CSV_FORMAT.md
@DEPLOY.md

Source files (read these to verify exact message strings -- do NOT rely on RESEARCH.md alone):
@apps/kerala_delivery/api/main.py (lines 82-89 for GEOCODING_REASON_MAP, 718-851 for file validation, 920-961 for geocoding error handling)
@core/data_import/csv_importer.py (lines 102-124 for _humanize_row_error, 214-265 for pre-validation, 278-302 for column validation, 388-397 for weight warning)
@core/data_import/cdcms_preprocessor.py (lines 352-372 for CDCMS validation errors)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CSV_FORMAT.md error tables to match code</name>
  <files>CSV_FORMAT.md</files>
  <action>
Read the source code files (main.py, csv_importer.py, cdcms_preprocessor.py) to verify exact message strings. Then update CSV_FORMAT.md "What Can Go Wrong" section with these specific changes:

**Before Processing table (2 stale entries to fix):**

1. Replace line 163 entry (missing address column):
   - OLD: `"Missing address column. Expected 'address' or one of ['address', 'delivery_address', 'addr', 'customer_address']. Found columns: [...]"`
   - NEW: `"Missing address column 'address' -- make sure you're uploading the correct file format"`
   - Keep the "Why It Happened" and "How to Fix It" columns appropriate to the new message

2. Replace line 164 entry (CDCMS required columns):
   - OLD: `"CDCMS export is missing required columns: {'OrderNo', 'ConsumerAddress'}. ... Make sure you're uploading the raw CDCMS export file."`
   - NEW: `"Required columns missing: ConsumerAddress, OrderNo -- make sure you're uploading the raw CDCMS export"`
   - No Python set notation `{...}` -- uses comma-separated sorted list with `--` separator

**During Processing table (add 6 missing _humanize_row_error entries):**

Keep the existing 3 rows (empty address, duplicate order_id, invalid weight). Add these 6 new rows AFTER the existing 3:

| What You See | Why It Happened | How to Fix It |
|---|---|---|
| "Invalid number value -- check for letters or symbols in numeric fields" | A numeric column (like quantity or weight) contains text instead of a number. | Remove letters or symbols from numeric columns. Use digits only (e.g., `14.2` not `14.2kg`). |
| "Unexpected value format -- check the cell contents" | A cell has an unexpected data format that the system cannot parse. | Check the cell for unusual characters or formatting. Clear the cell and re-type the value. |
| "Invalid value in this row" | A cell contains a value the system does not recognize. | Review the row for typos or incorrect data. |
| "Missing required field 'order_id' -- check your CSV has all required columns" | A required column is missing or the cell is empty in this row. | Make sure every row has values in the required columns. |
| "Empty or invalid cell -- fill in required fields" | A required cell is empty or contains only whitespace. | Fill in the empty cell with the correct value. |
| "Could not process this row -- check the data format" | The row has a data issue the system could not diagnose specifically. | Check all cells in this row for unusual formatting, then re-upload. |

**During Map Lookup table (replace all 6 rows with 7 correct entries):**

Replace the entire geocoding table with these 7 entries:

| What You See | Why It Happened | How to Fix It |
|---|---|---|
| "Address not found -- check spelling in CDCMS" | Google Maps could not find this address. The address may be misspelled or too vague. | Check the address for typos. Add more detail like the area name, post office, or nearby landmark. |
| "Geocoding service blocked -- contact IT" | The Google Maps service rejected the request. This is a system configuration issue, not a problem with your file. | Contact IT to check the Google Maps API key configuration. |
| "Google Maps quota exceeded -- contact IT" | Too many addresses were looked up in a short time, exceeding the Google Maps daily or per-second limit. | Contact IT. If urgent, wait a few minutes and try uploading again with fewer new addresses. |
| "Address could not be processed -- check for unusual characters" | The address contains characters or formatting that Google Maps cannot handle. | Simplify the address -- remove special characters, brackets, or unusual punctuation and use plain text. |
| "Google Maps is temporarily unavailable -- try again in a few minutes" | Google Maps experienced a temporary error. | Try uploading again in a few minutes. If the problem persists, contact IT. |
| "Geocoding service not configured (missing API key)" | The system does not have a Google Maps API key set up. Previously looked-up addresses still work from the cache. | Contact IT to configure the Google Maps API key. |
| "Could not find this address -- try checking the spelling" | Google Maps returned a status the system does not recognize, or the address lookup failed for an unknown reason. | Check the address for typos. Try adding more detail like the post office or landmark name. |

Do NOT change the table structure (keep 3 columns: What You See | Why It Happened | How to Fix It). Do NOT add code references -- this is a user-facing document.
  </action>
  <verify>
    <automated>grep -c "Address not found -- check spelling in CDCMS" CSV_FORMAT.md && grep -c "Required columns missing" CSV_FORMAT.md && grep -c "Could not process this row" CSV_FORMAT.md && grep -c "Missing required field" CSV_FORMAT.md && ! grep -q "{'OrderNo'" CSV_FORMAT.md && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <done>
    - CSV_FORMAT.md "Before Processing" table: 2 stale entries replaced with verbatim code strings (no set notation, uses -- separator)
    - CSV_FORMAT.md "During Processing" table: 9 total rows (3 existing + 6 new _humanize_row_error patterns)
    - CSV_FORMAT.md "During Map Lookup" table: 7 entries (6 GEOCODING_REASON_MAP + 1 fallback), all matching code verbatim
    - No Python set notation anywhere in CSV_FORMAT.md error tables
  </done>
</task>

<task type="auto">
  <name>Task 2: Update DEPLOY.md troubleshooting section</name>
  <files>DEPLOY.md</files>
  <action>
Update the Troubleshooting section (Section 5) in DEPLOY.md with these specific changes:

**1. KEEP as-is:** "Cannot connect to Docker" section (lines 188-199). This is an infrastructure message, not affected by Phase 17.

**2. REMOVE entirely:** "File not found" section (lines 201-207). This code path does not exist -- the dashboard uses drag-and-drop upload, not file paths. Delete the entire `### "File not found"` heading, code block, and fix paragraph.

**3. UPDATE "Missing required columns" section:**
   - Replace code block content:
     - OLD: `ValueError: CDCMS export is missing required columns: {'OrderNo'}`
     - NEW: `Required columns missing: OrderNo -- make sure you're uploading the raw CDCMS export`
   - No `ValueError:` prefix (user never sees Python exception types in the browser)
   - No `{...}` set notation
   - Update the **Fix:** paragraph to match: "The file is not a proper CDCMS export. Make sure you are exporting from the correct CDCMS page and the file has column headers like OrderNo, ConsumerAddress, etc."

**4. UPDATE "No orders remain after filtering" section:**
   - Replace code block content:
     - OLD: `WARNING: No orders remain after filtering`
     - NEW: `No 'Allocated-Printed' orders found in CDCMS export. Check that the file has orders with status 'Allocated-Printed'.`
   - This is the HTTPException message the user actually sees in the browser (not the logger.warning)
   - Update section heading to: `### "No Allocated-Printed orders found"`
   - Keep the **Fix:** paragraph about checking filter values and status

**5. UPDATE "Google Maps API key error" section:**
   - Replace code block content:
     - OLD: `ERROR: GOOGLE_MAPS_API_KEY not set`
     - NEW: `Geocoding service not configured (missing API key)`
   - This is the dashboard message (not the CLI script message)
   - Update section heading to: `### "Geocoding service not configured"`
   - Update **Fix:** paragraph: "Contact IT -- they need to configure the Google Maps API key in the system."

**6. ADD cross-link:** After the last troubleshooting subsection (before "System is slow"), add:

```markdown
> **For a complete list of error messages, see [CSV_FORMAT.md > What Can Go Wrong](CSV_FORMAT.md#what-can-go-wrong).**
```

**KEEP as-is:** "System is slow / not responding" and "How to update the system" sections. These are not error message sections.
  </action>
  <verify>
    <automated>! grep -q "FileNotFoundError" DEPLOY.md && ! grep -q "ValueError:" DEPLOY.md && ! grep -q "{'OrderNo'}" DEPLOY.md && ! grep -q "GOOGLE_MAPS_API_KEY not set" DEPLOY.md && grep -q "CSV_FORMAT.md" DEPLOY.md && grep -q "Required columns missing" DEPLOY.md && grep -q "Geocoding service not configured" DEPLOY.md && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <done>
    - "File not found" section removed entirely
    - "Missing required columns" shows humanized message without ValueError prefix or set notation
    - "No orders remain" shows the actual HTTPException message seen in the browser
    - "Google Maps API key" shows the dashboard message, not CLI script message
    - Cross-link to CSV_FORMAT.md added
    - No Python exception types (ValueError:, ERROR:, WARNING:) in any code block
  </done>
</task>

<task type="auto">
  <name>Task 3: Create ERROR-MAP.md traceability artifact</name>
  <files>.planning/phases/20-sync-error-message-documentation/ERROR-MAP.md</files>
  <action>
Read the source code files to verify every error message, then create ERROR-MAP.md in the phase directory. This is a verification artifact for the development team, NOT a user-facing document.

Use 3-column format: Message | Code Location | Status

Organize into 3 sections matching CSV_FORMAT.md structure:

**File-Level Errors (9 entries):**
Map each "Before Processing" table entry to its code location. Include:
- main.py:722 (unsupported file type)
- main.py:730 (unexpected content type)
- main.py:746 (file too large)
- main.py:771-772 (no Allocated-Printed orders)
- main.py:851 (no valid orders)
- csv_importer.py:301-302 (missing address column)
- cdcms_preprocessor.py:352-354 (required columns missing)
- cdcms_preprocessor.py:370-372 (empty ConsumerAddress)
- csv_importer.py:279 (unsupported file format)

**Row-Level Errors (9 entries):**
Map each "During Processing" table entry:
- csv_importer.py:221 (empty address)
- csv_importer.py:236-237 (duplicate order_id)
- csv_importer.py:395-396 (invalid weight warning)
- csv_importer.py:115 (invalid number value)
- csv_importer.py:117 (unexpected value format)
- csv_importer.py:118 (invalid value)
- csv_importer.py:121 (missing required field)
- csv_importer.py:123 (empty or invalid cell)
- csv_importer.py:124 (could not process row)

**Geocoding Errors (7 entries):**
Map each "During Map Lookup" table entry:
- main.py:83 (ZERO_RESULTS)
- main.py:84 (REQUEST_DENIED)
- main.py:85-86 (OVER_QUERY_LIMIT / OVER_DAILY_LIMIT -- single message)
- main.py:87 (INVALID_REQUEST)
- main.py:88 (UNKNOWN_ERROR)
- main.py:959 (no geocoder configured)
- main.py:934 (fallback)

All entries should have Status = "verified" after confirming against source code.

Include a header noting this was created during Phase 20, the verification date, and total entry count.
  </action>
  <verify>
    <automated>test -f .planning/phases/20-sync-error-message-documentation/ERROR-MAP.md && grep -c "verified" .planning/phases/20-sync-error-message-documentation/ERROR-MAP.md | xargs test 20 -le && echo "ARTIFACT EXISTS WITH VERIFIED ENTRIES"</automated>
  </verify>
  <done>
    - ERROR-MAP.md exists in phase directory
    - Contains 25 entries (9 file-level + 9 row-level + 7 geocoding)
    - Every entry has Message, Code Location, and Status columns
    - All entries marked "verified" after source code confirmation
    - No entries marked "stale"
  </done>
</task>

</tasks>

<verification>
After all tasks complete, verify end-to-end:

1. **No stale messages remain:**
   ```bash
   # No Python set notation in either doc
   ! grep -qE "\{'" CSV_FORMAT.md DEPLOY.md

   # No Python exception prefixes in DEPLOY.md code blocks
   ! grep -qE "^(ValueError|FileNotFoundError|ERROR|WARNING):" DEPLOY.md
   ```

2. **Message counts correct:**
   ```bash
   # CSV_FORMAT.md geocoding table should have 7 data rows (excluding header)
   # CSV_FORMAT.md row-level table should have 9 data rows
   # DEPLOY.md should NOT contain "File not found" section
   ```

3. **Cross-link exists:**
   ```bash
   grep "CSV_FORMAT.md" DEPLOY.md
   ```

4. **Traceability artifact complete:**
   ```bash
   # ERROR-MAP.md should have ~25 "verified" entries
   grep -c "verified" .planning/phases/20-sync-error-message-documentation/ERROR-MAP.md
   ```
</verification>

<success_criteria>
- All error message strings in CSV_FORMAT.md match actual code output verbatim
- DEPLOY.md troubleshooting uses humanized messages (no Python internals)
- CSV_FORMAT.md geocoding table has exactly 7 entries
- CSV_FORMAT.md row-level table has exactly 9 entries
- DEPLOY.md "File not found" section is gone
- DEPLOY.md has cross-link to CSV_FORMAT.md
- ERROR-MAP.md maps every documented message to code file:line with "verified" status
</success_criteria>

<output>
After completion, create `.planning/phases/20-sync-error-message-documentation/20-01-SUMMARY.md`
</output>
