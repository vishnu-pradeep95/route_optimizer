---
phase: 04-documentation-accuracy-refresh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/ERROR-MAP.md
  - apps/kerala_delivery/dashboard/src/pages/FleetManagement.tsx
  - apps/kerala_delivery/api/main.py
  - apps/kerala_delivery/config.py
  - core/licensing/__init__.py
  - core/database/repository.py
  - infra/postgres/init.sql
autonomous: true
requirements: [DOC-VALIDATE, DOC-CLEANUP]

must_haves:
  truths:
    - "Every main.py line reference in ERROR-MAP.md points to the correct line in the current source"
    - "Every core/ file line reference in ERROR-MAP.md points to the correct line in the current source"
    - "No source code file contains a reference to plan/kerala_delivery_route_system_design.md or plan/session-journal.md"
    - "ERROR-MAP.md Verified date is 2026-03-10"
  artifacts:
    - path: "docs/ERROR-MAP.md"
      provides: "Accurate error traceability map with 25 correct line references"
      contains: "Verified: 2026-03-10"
    - path: "apps/kerala_delivery/dashboard/src/pages/FleetManagement.tsx"
      provides: "Clean JSDoc comments without stale plan/ references"
    - path: "apps/kerala_delivery/api/main.py"
      provides: "Clean comments without stale plan/ or session-journal references"
    - path: "apps/kerala_delivery/config.py"
      provides: "Clean docstring and comments without stale plan/ references"
    - path: "core/licensing/__init__.py"
      provides: "Clean module docstring without stale plan/ reference"
    - path: "core/database/repository.py"
      provides: "Clean comment without stale plan/ reference"
    - path: "infra/postgres/init.sql"
      provides: "Clean header without stale plan/ reference"
  key_links:
    - from: "docs/ERROR-MAP.md"
      to: "apps/kerala_delivery/api/main.py"
      via: "line number references"
      pattern: "main\\.py:\\d+"
    - from: "docs/ERROR-MAP.md"
      to: "core/data_import/csv_importer.py"
      via: "line number references"
      pattern: "csv_importer\\.py:\\d+"
---

<objective>
Fix documentation drift in ERROR-MAP.md (12 stale line-number references) and remove all 9 stale plan/ directory references from 6 source code files.

Purpose: Phase 02 added ~140 lines to main.py, shifting line numbers in ERROR-MAP.md. Phase 01 deleted the plan/ directory but left 9 references in source code comments. Both are documentation accuracy issues that erode traceability.
Output: Accurate ERROR-MAP.md with all 25 entries verified, and zero stale plan/ references in source code.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-documentation-accuracy-refresh/04-RESEARCH.md

@docs/ERROR-MAP.md
@apps/kerala_delivery/api/main.py
@apps/kerala_delivery/dashboard/src/pages/FleetManagement.tsx
@apps/kerala_delivery/config.py
@core/licensing/__init__.py
@core/database/repository.py
@infra/postgres/init.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ERROR-MAP.md line references and verified date</name>
  <files>docs/ERROR-MAP.md</files>
  <action>
Update all 12 drifted line-number references in docs/ERROR-MAP.md to match the actual current source code. The research phase has already verified every entry. Apply these exact corrections:

**File-Level Errors (main.py) -- 5 entries:**
1. Entry "Unsupported file type (.pdf)..." -- change `main.py:722` to `main.py:863`
2. Entry "Unexpected content type..." -- change `main.py:730` to `main.py:874`
3. Entry "File too large..." -- change `main.py:746` to `main.py:893`
4. Entry "No 'Allocated-Printed' orders..." -- change `main.py:771-772` to `main.py:921` (now single line, not a range)
5. Entry "No valid orders found..." -- change `main.py:851` to `main.py:1008`

**Geocoding Errors (main.py) -- 7 entries:**
6. Entry "Address not found..." -- change `main.py:83` to `main.py:94`
7. Entry "Geocoding service blocked..." -- change `main.py:84` to `main.py:95`
8. Entry "Google Maps quota exceeded..." -- change `main.py:85-86` to `main.py:96-97`
9. Entry "Address could not be processed..." -- change `main.py:87` to `main.py:98`
10. Entry "Google Maps is temporarily unavailable..." -- change `main.py:88` to `main.py:99`
11. Entry "Geocoding service not configured..." -- change `main.py:959` to `main.py:1125`
12. Entry "Could not find this address..." -- change `main.py:934` to `main.py:1100`

**Leave all 13 core/ file entries UNCHANGED** -- they are already correct per research verification.

**Update the header:** Change `**Verified:** 2026-03-07` to `**Verified:** 2026-03-10`.

**IMPORTANT:** Before writing, use `grep -n` on main.py to spot-check at least 3 entries (one from each group: file-level, geocoding map, geocoding fallback) to confirm the research line numbers are still accurate. If any have shifted since the research was done, use the grep result instead.
  </action>
  <verify>
    <automated>cd /home/vishnu/projects/routing_opt && grep -c "main.py:722\|main.py:730\|main.py:746\|main.py:771\|main.py:851\|main.py:83)\|main.py:84)\|main.py:85-86\|main.py:87)\|main.py:88)\|main.py:959\|main.py:934" docs/ERROR-MAP.md | grep -q "^0$" && echo "PASS: No old line numbers remain" || echo "FAIL: Old line numbers still present"</automated>
  </verify>
  <done>All 25 ERROR-MAP.md entries have correct line references matching the current source code. Verified date is 2026-03-10. Zero old/stale line numbers remain.</done>
</task>

<task type="auto">
  <name>Task 2: Remove all stale plan/ directory references from source code</name>
  <files>
apps/kerala_delivery/dashboard/src/pages/FleetManagement.tsx
apps/kerala_delivery/api/main.py
apps/kerala_delivery/config.py
core/licensing/__init__.py
core/database/repository.py
infra/postgres/init.sql
  </files>
  <action>
Remove or replace all 9 stale references to `plan/kerala_delivery_route_system_design.md` and `plan/session-journal.md` across 6 source files. Follow the replacement strategy from research:

**1. FleetManagement.tsx line 53** -- `Source: plan/kerala_delivery_route_system_design.md, Section 3`
Remove the `Source:` line entirely. The surrounding JSDoc already explains the 90% safety factor. Keep the `Also: apps/kerala_delivery/config.py VEHICLE_MAX_WEIGHT_KG` line but change "Also:" to "See:" since it becomes the only cross-reference.

**2. FleetManagement.tsx line 74** -- `Source: plan/kerala_delivery_route_system_design.md, Safety Constraints`
Remove the `Source:` line entirely. The surrounding JSDoc already mentions the Kerala MVD directive.

**3. main.py line 490** -- `# See: plan/session-journal.md (OPEN: C1 auth discussion)`
Remove the entire comment line. The C1 auth discussion is historical context that no longer needs a source link.

**4. main.py line 1018** -- `# See: plan/kerala_delivery_route_system_design.md (Safety Constraints)`
Remove the entire comment line. The surrounding code context (minimum delivery window) is self-explanatory.

**5. config.py line 11** -- `- Design doc: plan/kerala_delivery_route_system_design.md` (in module docstring)
Remove this line from the docstring. The config module IS the canonical source for these values.

**6. config.py line 139** -- `# See: plan/kerala_delivery_route_system_design.md, Section 10 (GPS drift risk)`
Remove the entire comment line. The GPS_ACCURACY_THRESHOLD_M constant is self-documenting with its existing inline comment.

**7. core/licensing/__init__.py line 30** -- `See: plan/kerala_delivery_route_system_design.md, Phase 4C`
Remove this line from the module docstring.

**8. core/database/repository.py line 424** -- `# See: plan/kerala_delivery_route_system_design.md, Section 10 (GPS drift risk)`
Remove the entire comment line. The GPS drift filter logic is self-explanatory in context.

**9. infra/postgres/init.sql line 8** -- `-- Design reference: plan/kerala_delivery_route_system_design.md, Section 5`
Remove the entire comment line. The schema IS the implementation; a design doc reference adds no value.

**After all edits:** Run `grep -rn "plan/kerala_delivery_route_system_design\|plan/session-journal" --include="*.py" --include="*.tsx" --include="*.ts" --include="*.sql" --include="*.js"` from the project root to confirm zero remaining references in source code.
  </action>
  <verify>
    <automated>cd /home/vishnu/projects/routing_opt && grep -rn "plan/kerala_delivery_route_system_design\|plan/session-journal" --include="*.py" --include="*.tsx" --include="*.ts" --include="*.sql" --include="*.js" apps/ core/ infra/ | grep -v ".planning/" | wc -l | grep -q "^0$" && echo "PASS: No stale plan/ references in source code" || echo "FAIL: Stale plan/ references still exist"</automated>
  </verify>
  <done>Zero references to plan/kerala_delivery_route_system_design.md or plan/session-journal.md remain in any source code file (*.py, *.tsx, *.ts, *.sql, *.js).</done>
</task>

</tasks>

<verification>
1. ERROR-MAP.md contains zero old line numbers (all 12 updated entries reflect current source)
2. ERROR-MAP.md "Verified" date reads 2026-03-10
3. grep for `plan/kerala_delivery_route_system_design` and `plan/session-journal` across source code returns zero matches
4. No syntax errors introduced in any modified file (Python files parse, TSX compiles, SQL is valid)
</verification>

<success_criteria>
- All 25 ERROR-MAP.md entries have verified-correct line references
- Zero stale plan/ directory references exist in source code
- No functional changes to any code (comment-only edits)
</success_criteria>

<output>
After completion, create `.planning/phases/04-documentation-accuracy-refresh/04-01-SUMMARY.md`
</output>
