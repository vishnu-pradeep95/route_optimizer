---
phase: 01-documentation-restructure-validation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - README.md
  - docs/DEPLOY.md
  - docs/SETUP.md
  - docs/GUIDE.md
  - docs/CSV_FORMAT.md
  - docs/DISTRIBUTION.md
  - docs/ENV-COMPARISON.md
  - docs/GOOGLE-MAPS.md
  - docs/LICENSING.md
  - docs/ATTRIBUTION.md
  - docs/ERROR-MAP.md
  - docs/INDEX.md
  - CLAUDE.md
autonomous: true
requirements:
  - DOC-VALIDATE
  - DOC-AUDIENCE
  - DOC-INDEX
  - DOC-README

must_haves:
  truths:
    - "Every command, file path, endpoint, and env var mentioned in docs matches the actual codebase"
    - "Each document has a header audience badge (Office Employee, Developer, or Both)"
    - "docs/INDEX.md exists with a table of all docs, descriptions, and audience tags"
    - "README.md is overview-only -- no setup/install instructions, links to docs/ for details"
    - "CLAUDE.md test checklist and file paths are accurate for current codebase"
    - "GUIDE.md stale content (Phase 4 refs, outdated test counts, plan/ tree) is fixed"
    - "Office-employee docs use plain English -- no unexplained jargon"
  artifacts:
    - path: "docs/INDEX.md"
      provides: "Table of contents with audience tags for all documentation"
      min_lines: 15
    - path: "README.md"
      provides: "Overview-only project README linking to docs/ for details"
      contains: "docs/DEPLOY.md"
  key_links:
    - from: "docs/INDEX.md"
      to: "docs/*.md"
      via: "relative markdown links"
      pattern: "\\[.*\\]\\(.*\\.md\\)"
    - from: "README.md"
      to: "docs/DEPLOY.md"
      via: "employee redirect link"
      pattern: "docs/DEPLOY\\.md"
---

<objective>
Validate all documentation content against the actual codebase, fix drift, add audience badges, create INDEX.md, and trim README.md to overview-only.

Purpose: Ensure every documented command, path, endpoint, and env var is accurate post-v1.4. Add audience separation so office employees and developers can find the right docs instantly.

Output: All docs validated and corrected, audience badges on every doc, INDEX.md created, README.md trimmed.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-documentation-restructure-validation/01-CONTEXT.md
@.planning/phases/01-documentation-restructure-validation/01-RESEARCH.md
@.planning/phases/01-documentation-restructure-validation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Systematic drift validation and content fixes across all docs</name>
  <files>
    docs/DEPLOY.md, docs/SETUP.md, docs/GUIDE.md, docs/CSV_FORMAT.md,
    docs/DISTRIBUTION.md, docs/ENV-COMPARISON.md, docs/GOOGLE-MAPS.md,
    docs/LICENSING.md, docs/ATTRIBUTION.md, docs/ERROR-MAP.md, CLAUDE.md
  </files>
  <action>
    Perform systematic validation of ALL documentation against the actual codebase. Use grep/find to verify -- do NOT rely on memory.

    **1. Validate commands in docs:**
    - Extract all `docker compose` commands and verify service names match `docker-compose.yml` and `docker-compose.prod.yml`
    - Verify `./scripts/start.sh`, `./scripts/bootstrap.sh`, `./scripts/reset.sh` etc. exist and are referenced correctly
    - Verify `python scripts/import_orders.py` and `python scripts/geocode_batch.py` exist
    - Run `pytest tests/ --collect-only -q 2>/dev/null | tail -1` to get actual test count and update GUIDE.md (currently says "351 tests" in multiple places at lines ~424, 444, 580)

    **2. Validate file paths in docs:**
    - Grep all file paths referenced in each doc, check each exists in the codebase
    - Key check: GUIDE.md references `infra/alembic/alembic.ini` -- actual path is `alembic.ini` at root. Fix if wrong.
    - Key check: GUIDE.md directory tree (line ~252) -- update to remove `plan/` and reflect current structure
    - Verify all `core/` paths mentioned in GUIDE.md exist
    - Verify all `scripts/` references across docs match actual `ls scripts/`

    **3. Validate API endpoints:**
    - Extract all endpoints from CLAUDE.md test checklist
    - Cross-reference against `grep -n "@app\.\(get\|post\|put\|delete\)" apps/kerala_delivery/api/main.py`
    - Fix any mismatches

    **4. Validate environment variables:**
    - Extract all env vars mentioned across docs
    - Cross-reference against `.env.example` contents
    - Verify `CORS_ALLOWED_ORIGINS`, `RATE_LIMIT_ENABLED` documented correctly

    **5. Fix GUIDE.md stale content (from research Pitfall 4):**
    - Section 12 "What's Built vs. What's Planned" (lines ~557-601): Remove entirely or replace with a brief note linking to ROADMAP.md. Project is at Phase 24, not Phase 4.
    - "Need Help?" section (lines ~640-644): Remove dead links to plan/ files (already handled in Plan 01), clean up remaining content
    - Section 9 setup duplication (lines ~397-446): Replace with a brief pointer to SETUP.md ("For setup instructions, see [SETUP.md](SETUP.md)")
    - Update test count references to actual count

    **6. Fix OSRM health endpoint contradiction (from research Open Question 2):**
    - README.md line ~416 says "OSRM has no /health endpoint"
    - GUIDE.md line ~442 says to check `curl http://localhost:5000/health`
    - Verify which is correct: `grep -n "health\|5000" apps/kerala_delivery/api/main.py scripts/start.sh`
    - Fix the incorrect doc

    **7. Validate CLAUDE.md specifically:**
    - Verify driver PWA path `apps/kerala_delivery/driver_app/` exists
    - Verify Tailwind v4 prefix conventions (`tw:` colon syntax) match codebase
    - Verify API endpoint list matches actual endpoints
    - Verify test checklist items are still accurate for current PWA state

    **8. GOOGLE-MAPS.md per user decision:**
    - Ensure it is a generic troubleshooting guide
    - Remove any references to the current invalid API key situation (REQUEST_DENIED)
    - Keep it useful as general API key setup/troubleshooting

    **9. DISTRIBUTION.md:**
    - Verify included/excluded file tables match `scripts/build-dist.sh` rsync exclude patterns
    - Update paths to reflect docs/ structure (partially done in Plan 01, but verify content accuracy here)

    **10. Office-employee docs language check (DEPLOY.md, CSV_FORMAT.md):**
    - Scan for technical jargon without plain-English explanation
    - "Start the system" not "docker compose up" pattern -- ensure plain English is primary, technical command secondary
    - Parenthetical explanations for any unavoidable technical terms
  </action>
  <verify>
    <automated>grep -c "351 tests" docs/GUIDE.md | grep -q "^0$" && echo "PASS: stale test count fixed" || echo "FAIL: 351 tests still present"; grep -c "Phase 4" docs/GUIDE.md | grep -q "^0$" && echo "PASS: Phase 4 ref removed" || echo "FAIL: Phase 4 still referenced"; grep -rn "plan/" docs/GUIDE.md | wc -l | grep -q "^0$" && echo "PASS: no plan/ in GUIDE" || echo "FAIL: plan/ still in GUIDE"</automated>
  </verify>
  <done>Every command, file path, endpoint, and env var in all docs is verified against the actual codebase. GUIDE.md stale sections removed/updated. OSRM health contradiction resolved. CLAUDE.md verified accurate. Office-employee docs use plain English.</done>
</task>

<task type="auto">
  <name>Task 2: Add audience badges, create INDEX.md, and trim README.md</name>
  <files>
    docs/INDEX.md, README.md,
    docs/DEPLOY.md, docs/SETUP.md, docs/GUIDE.md, docs/CSV_FORMAT.md,
    docs/DISTRIBUTION.md, docs/ENV-COMPARISON.md, docs/GOOGLE-MAPS.md,
    docs/LICENSING.md, docs/ATTRIBUTION.md, docs/ERROR-MAP.md
  </files>
  <action>
    **1. Add audience badge to EVERY doc in docs/ (first line after title):**

    Use blockquote format per user decision:
    - `docs/DEPLOY.md`: `> **Audience:** Office Employee`
    - `docs/CSV_FORMAT.md`: `> **Audience:** Office Employee | Developer`
    - `docs/GOOGLE-MAPS.md`: `> **Audience:** Office Employee | Developer`
    - `docs/SETUP.md`: `> **Audience:** Developer`
    - `docs/GUIDE.md`: `> **Audience:** Developer`
    - `docs/DISTRIBUTION.md`: `> **Audience:** Developer`
    - `docs/ENV-COMPARISON.md`: `> **Audience:** Developer`
    - `docs/LICENSING.md`: `> **Audience:** Developer`
    - `docs/ATTRIBUTION.md`: `> **Audience:** Developer`
    - `docs/ERROR-MAP.md`: `> **Audience:** Developer`

    Place the badge immediately after the first `# Title` line, with a blank line before and after.

    **2. Create docs/INDEX.md:**

    Create a documentation index with:
    - Title: `# Documentation Index`
    - Brief intro paragraph explaining the audience tags
    - Table with columns: Document, Description, Audience
    - Office Employee docs listed first, then Both, then Developer
    - All links are relative (same directory): `[DEPLOY.md](DEPLOY.md)`
    - Include a note at the top: office employees should start with DEPLOY.md

    Use this ordering and content (per research INDEX.md template, adjusted):
    ```
    | Document | Description | Audience |
    |----------|-------------|----------|
    | [DEPLOY.md](DEPLOY.md) | System setup and daily operations for office staff | Office Employee |
    | [CSV_FORMAT.md](CSV_FORMAT.md) | Upload file format reference and column guide | Both |
    | [GOOGLE-MAPS.md](GOOGLE-MAPS.md) | Google Maps API setup and troubleshooting | Both |
    | [SETUP.md](SETUP.md) | Developer environment setup and configuration | Developer |
    | [GUIDE.md](GUIDE.md) | Platform architecture, code organization, and learning path | Developer |
    | [DISTRIBUTION.md](DISTRIBUTION.md) | Build and package customer distribution archives | Developer |
    | [LICENSING.md](LICENSING.md) | License key generation, activation, and lifecycle | Developer |
    | [ENV-COMPARISON.md](ENV-COMPARISON.md) | Development vs production environment differences | Developer |
    | [ATTRIBUTION.md](ATTRIBUTION.md) | Third-party software licenses and attribution | Developer |
    | [ERROR-MAP.md](ERROR-MAP.md) | Error message traceability from UI to source code | Developer |
    ```

    **3. Trim README.md to overview-only:**

    Per user decision and research Pitfall 5:
    - REMOVE the Quick Start section (lines ~13-55) -- setup/install instructions
    - REMOVE the "Stopping & Restarting" section if present
    - KEEP: Project description/overview, architecture overview, API endpoints table, contributing section
    - ADD near the top (after the project description): An employee redirect callout using the existing pattern:
      `> **Employee?** Start with [DEPLOY.md](docs/DEPLOY.md) for setup and daily operations.`
    - UPDATE the documentation table/links section to point to `docs/INDEX.md` as the entry point
    - Ensure README is clearly developer-focused per user decision
  </action>
  <verify>
    <automated>test -f docs/INDEX.md && echo "PASS: INDEX exists" || echo "FAIL: no INDEX"; grep -c "Audience:" docs/*.md | awk -F: '{sum+=$2} END {print (sum>=10 ? "PASS: all badges" : "FAIL: missing badges") ": " sum "/10"}'; grep -q "docs/DEPLOY.md" README.md && echo "PASS: README links to docs" || echo "FAIL: README missing docs links"; grep -c "Quick Start\|quick start" README.md | grep -q "^0$" && echo "PASS: Quick Start removed" || echo "FAIL: Quick Start still present"</automated>
  </verify>
  <done>All 10 docs have audience badges. docs/INDEX.md exists with complete table. README.md is overview-only with employee redirect to docs/DEPLOY.md. No setup/install instructions remain in README.</done>
</task>

</tasks>

<verification>
1. `grep -c "Audience:" docs/*.md` -- all 10 docs have audience badges
2. `test -f docs/INDEX.md` -- INDEX exists
3. `grep -c "docs/" README.md` returns >= 5 (links to docs/)
4. `grep -c "351 tests" docs/GUIDE.md` returns 0 (stale count fixed)
5. `grep -c "Phase 4" docs/GUIDE.md` returns 0 (stale phase ref removed)
6. `grep -c "Quick Start" README.md` returns 0 (setup removed from README)
7. All file paths mentioned in docs exist in actual codebase (spot-check 10 random paths)
</verification>

<success_criteria>
- Every documented command, file path, endpoint, and env var matches the actual codebase
- All 10 docs in docs/ have audience badges in correct format
- docs/INDEX.md exists with complete table of all docs, descriptions, and audience tags
- README.md contains no setup/install instructions -- overview only
- README.md has employee redirect to docs/DEPLOY.md
- GUIDE.md stale content (Phase 4, 351 tests, plan/ tree, outdated sections) is fixed
- OSRM health endpoint contradiction between README and GUIDE is resolved
- CLAUDE.md verified accurate for current codebase state
- Office-employee docs (DEPLOY.md, CSV_FORMAT.md) use plain English
</success_criteria>

<output>
After completion, create `.planning/phases/01-documentation-restructure-validation/01-02-SUMMARY.md`
</output>
