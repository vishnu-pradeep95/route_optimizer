---
phase: 01-documentation-restructure-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/DEPLOY.md
  - docs/SETUP.md
  - docs/GUIDE.md
  - docs/CSV_FORMAT.md
  - docs/DISTRIBUTION.md
  - docs/ENV-COMPARISON.md
  - docs/GOOGLE-MAPS.md
  - docs/LICENSING.md
  - docs/ATTRIBUTION.md
  - docs/ERROR-MAP.md
  - README.md
  - GUIDE.md
  - .github/instructions/git-integration.instructions.md
  - .github/instructions/continuation-format.instructions.md
  - .github/agents/codebase-mapper.agent.md
  - .github/agents/plan-checker.agent.md
  - .github/agents/session-journal.agent.md
  - .github/skills/diagnose-issues/SKILL.md
  - .github/copilot-instructions.md
  - .github/agents/kerala-delivery-route-architect.agent.md
autonomous: true
requirements:
  - DOC-MOVE
  - DOC-XREF
  - DOC-CLEANUP

must_haves:
  truths:
    - "All documentation files (except README.md and CLAUDE.md) reside in docs/ directory"
    - "No broken internal markdown links exist across the repository"
    - "The plan/ directory no longer exists in the repository"
    - "All .github/ references to plan/ are updated or removed"
  artifacts:
    - path: "docs/DEPLOY.md"
      provides: "Office employee deployment guide (moved from root)"
    - path: "docs/SETUP.md"
      provides: "Developer setup guide (moved from root)"
    - path: "docs/GUIDE.md"
      provides: "Platform guide (moved from root)"
    - path: "docs/CSV_FORMAT.md"
      provides: "CSV format reference (moved from root)"
    - path: "docs/DISTRIBUTION.md"
      provides: "Distribution build workflow (moved from root)"
    - path: "docs/ENV-COMPARISON.md"
      provides: "Environment comparison (moved from root)"
    - path: "docs/GOOGLE-MAPS.md"
      provides: "API troubleshooting (moved from root)"
    - path: "docs/LICENSING.md"
      provides: "License lifecycle docs (moved from root)"
    - path: "docs/ATTRIBUTION.md"
      provides: "Third-party attribution (moved from root)"
    - path: "docs/ERROR-MAP.md"
      provides: "Error message traceability (copied from milestone)"
  key_links:
    - from: "README.md"
      to: "docs/*.md"
      via: "markdown links with docs/ prefix"
      pattern: "\\(docs/[A-Z].*\\.md\\)"
    - from: "docs/GUIDE.md"
      to: "../README.md"
      via: "relative parent path"
      pattern: "\\(\\.\\./"
---

<objective>
Move all documentation files into docs/ directory, update every cross-reference, delete stale plan/ directory, and clean up .github/ references.

Purpose: Restructure the repo root from 10 scattered .md files to a clean docs/ directory, preserving git history via `git mv` and ensuring zero broken links.

Output: All docs in docs/, all cross-references updated, plan/ deleted, .github/ cleaned.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-documentation-restructure-validation/01-CONTEXT.md
@.planning/phases/01-documentation-restructure-validation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move files to docs/ and delete plan/</name>
  <files>
    docs/DEPLOY.md, docs/SETUP.md, docs/GUIDE.md, docs/CSV_FORMAT.md,
    docs/DISTRIBUTION.md, docs/ENV-COMPARISON.md, docs/GOOGLE-MAPS.md,
    docs/LICENSING.md, docs/ATTRIBUTION.md, docs/ERROR-MAP.md
  </files>
  <action>
    1. Create docs/ directory: `mkdir -p docs/`

    2. Move 9 root-level doc files using `git mv` to preserve history:
       - `git mv DEPLOY.md docs/DEPLOY.md`
       - `git mv SETUP.md docs/SETUP.md`
       - `git mv GUIDE.md docs/GUIDE.md`
       - `git mv CSV_FORMAT.md docs/CSV_FORMAT.md`
       - `git mv DISTRIBUTION.md docs/DISTRIBUTION.md`
       - `git mv ENV-COMPARISON.md docs/ENV-COMPARISON.md`
       - `git mv GOOGLE-MAPS.md docs/GOOGLE-MAPS.md`
       - `git mv LICENSING.md docs/LICENSING.md`
       - `git mv ATTRIBUTION.md docs/ATTRIBUTION.md`

    3. Copy ERROR-MAP.md from its ACTUAL location (NOT apps/kerala_delivery/api/):
       `cp .planning/milestones/v1.3-phases/20-sync-error-message-documentation/ERROR-MAP.md docs/ERROR-MAP.md`
       `git add docs/ERROR-MAP.md`

    4. Delete plan/ directory entirely:
       `git rm -r plan/`

    5. Verify: Count files in docs/ (should be 10), confirm plan/ gone, confirm no root-level .md besides README.md and CLAUDE.md.
  </action>
  <verify>
    <automated>ls docs/*.md | wc -l | grep -q "10" && echo "PASS: 10 docs" || echo "FAIL"; test ! -d plan && echo "PASS: plan/ deleted" || echo "FAIL: plan/ exists"; ls *.md | sort | diff - <(echo -e "CLAUDE.md\nREADME.md") && echo "PASS: only README+CLAUDE at root" || echo "FAIL: extra .md at root"</automated>
  </verify>
  <done>10 files in docs/, plan/ deleted, only README.md and CLAUDE.md remain at root</done>
</task>

<task type="auto">
  <name>Task 2: Update all cross-references across the repository</name>
  <files>
    README.md,
    docs/GUIDE.md,
    docs/DISTRIBUTION.md,
    .github/copilot-instructions.md,
    .github/agents/kerala-delivery-route-architect.agent.md,
    .github/instructions/git-integration.instructions.md,
    .github/instructions/continuation-format.instructions.md,
    .github/agents/codebase-mapper.agent.md,
    .github/agents/plan-checker.agent.md,
    .github/agents/session-journal.agent.md,
    .github/skills/diagnose-issues/SKILL.md
  </files>
  <action>
    **README.md cross-reference updates (9 links):**
    Update all root-relative doc links to add `docs/` prefix. Per the research cross-reference inventory:
    - `DEPLOY.md` -> `docs/DEPLOY.md` (lines ~9, 524)
    - `SETUP.md` -> `docs/SETUP.md` (lines ~57, 416, 516, 525)
    - `LICENSING.md` -> `docs/LICENSING.md` (lines ~457, 526)
    - `DISTRIBUTION.md` -> `docs/DISTRIBUTION.md` (line ~527)
    - `ENV-COMPARISON.md` -> `docs/ENV-COMPARISON.md` (line ~528)
    - `GOOGLE-MAPS.md` -> `docs/GOOGLE-MAPS.md` (line ~529)
    - `ATTRIBUTION.md` -> `docs/ATTRIBUTION.md` (line ~530)
    - `CSV_FORMAT.md` -> `docs/CSV_FORMAT.md` (line ~531)
    - `GUIDE.md` -> `docs/GUIDE.md` (line ~532)
    - REMOVE the `plan/kerala_delivery_route_system_design.md` link at line ~540 (plan/ deleted)

    **docs/GUIDE.md cross-reference updates:**
    - Remove references to `plan/kerala_delivery_route_system_design.md` (line ~640) and `plan/session-journal.md` (line ~641) -- these files no longer exist
    - Change `[README.md](README.md)` to `[README.md](../README.md)` (line ~643) -- GUIDE.md is now in docs/, README is at root
    - `[SETUP.md](SETUP.md)` stays UNCHANGED -- both files are now in same docs/ directory
    - Remove `plan/` from the directory tree listing if present (line ~252)

    **docs/DISTRIBUTION.md updates:**
    - Update the "What's included" and "What's excluded" tables (lines ~49-78) to reference `docs/` paths instead of root paths
    - Remove `plan/` from excluded list (no longer exists)

    **.github/ file cleanup (15+ stale plan/ references):**
    For each file listed in research Pitfall 3:
    - `.github/instructions/git-integration.instructions.md` (1 ref): Update `plan/` reference to `.planning/PROJECT.md` or remove
    - `.github/instructions/continuation-format.instructions.md` (2 refs): Remove plan/ references
    - `.github/agents/codebase-mapper.agent.md` (1 ref): Remove plan/ reference
    - `.github/agents/plan-checker.agent.md` (6 refs): Remove/update plan/ references
    - `.github/agents/session-journal.agent.md` (4 refs): Remove/update plan/ references (this agent references `plan/session-journal.md` which is deleted)
    - `.github/skills/diagnose-issues/SKILL.md` (1 ref): Remove plan/ reference
    - `.github/copilot-instructions.md` (2 refs to SETUP.md): Update to `docs/SETUP.md`
    - `.github/agents/kerala-delivery-route-architect.agent.md` (2 refs to SETUP.md): Update to `docs/SETUP.md`

    **Important:** Do NOT change links between docs/ files (e.g., DEPLOY.md -> CSV_FORMAT.md stays as-is since they are in the same directory).

    **Verification after all changes:** Run a grep to confirm zero remaining references to `plan/kerala` or `plan/session` across the entire repo (excluding .planning/ and .git/).
  </action>
  <verify>
    <automated>grep -rn "plan/kerala\|plan/session" --include="*.md" --exclude-dir=.planning --exclude-dir=.git . | grep -v ".planning/" | wc -l | grep -q "^0$" && echo "PASS: no stale plan/ refs" || echo "FAIL: stale plan/ refs remain"; grep -rn "\(DEPLOY\.md\)" README.md | grep -v "docs/" | wc -l | grep -q "^0$" && echo "PASS: README links updated" || echo "FAIL: README links not updated"</automated>
  </verify>
  <done>Zero broken cross-references. All README.md doc links point to docs/. All .github/ plan/ references removed. GUIDE.md links to ../README.md. DISTRIBUTION.md tables reflect docs/ paths.</done>
</task>

</tasks>

<verification>
1. `ls docs/*.md | wc -l` returns 10
2. `test ! -d plan` succeeds
3. `ls *.md` shows only README.md and CLAUDE.md at root
4. `grep -rn "plan/kerala\|plan/session" --include="*.md" --exclude-dir=.planning --exclude-dir=.git .` returns nothing
5. `grep -c "docs/" README.md` returns >= 9 (all doc links updated)
6. `grep "../README.md" docs/GUIDE.md` finds at least 1 match
</verification>

<success_criteria>
- 10 documentation files in docs/ directory with git history preserved
- plan/ directory completely removed
- Zero broken markdown links across the repository
- All .github/ agent files cleaned of stale plan/ references
- DISTRIBUTION.md tables reflect new docs/ structure
</success_criteria>

<output>
After completion, create `.planning/phases/01-documentation-restructure-validation/01-01-SUMMARY.md`
</output>
