---
phase: 08-api-dead-code-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/kerala_delivery/api/main.py
  - apps/kerala_delivery/config.py
  - core/geocoding/cache.py
  - core/geocoding/normalize.py
  - tests/apps/kerala_delivery/api/test_api.py
  - tests/apps/kerala_delivery/test_config.py
  - scripts/compare_routes.py
autonomous: true
requirements: [API-01, API-02, API-03, API-04, API-05]

must_haves:
  truths:
    - "`_build_fleet()` function does not exist in `main.py` and no reference to it exists anywhere in the codebase"
    - "All imports in `main.py` are at file top -- no mid-file imports"
    - "No unused imports remain in `main.py` (no `Route`, `RouteAssignment`, `Vehicle` from core.models after `_build_fleet` removal)"
    - "`OSRM_URL` is not defined in `config.py`"
    - "`cache.py` docstring accurately describes the normalization-based lookup (no SHA-256 reference)"
  artifacts:
    - path: "apps/kerala_delivery/api/main.py"
      provides: "Clean imports, no dead code"
    - path: "apps/kerala_delivery/config.py"
      provides: "No unused OSRM_URL variable"
    - path: "core/geocoding/cache.py"
      provides: "Corrected docstring"
    - path: "core/geocoding/normalize.py"
      provides: "Corrected historical docstring reference"
  key_links:
    - from: "tests/apps/kerala_delivery/api/test_api.py"
      to: "apps/kerala_delivery/api/main.py"
      via: "Mock patches must not reference _build_fleet"
      pattern: "_build_fleet"
    - from: "scripts/compare_routes.py"
      to: "apps/kerala_delivery/config.py"
      via: "Script must read OSRM_URL from env directly, not config.OSRM_URL"
      pattern: "config\\.OSRM_URL"
---

<objective>
Remove dead code, consolidate imports, delete unused config, and correct stale docstrings across the API codebase.

Purpose: Eliminate tech debt identified during v1.1 development -- dead `_build_fleet()` function, unused imports, mid-file import, unused `OSRM_URL` config var, and inaccurate SHA-256 docstring.

Output: Clean `main.py` with zero dead code and all imports at top, clean `config.py` without dead OSRM_URL, accurate `cache.py`/`normalize.py` docstrings, updated tests and scripts.
</objective>

<execution_context>
@/home/vishnu/.claude/get-shit-done/workflows/execute-plan.md
@/home/vishnu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@apps/kerala_delivery/api/main.py
@apps/kerala_delivery/config.py
@core/geocoding/cache.py
@core/geocoding/normalize.py
@tests/apps/kerala_delivery/api/test_api.py
@tests/apps/kerala_delivery/test_config.py
@scripts/compare_routes.py

<interfaces>
<!-- Key code locations the executor needs. -->

From apps/kerala_delivery/api/main.py:
- Lines 647-666: `_build_fleet()` function to DELETE entirely (definition + comment block lines 644-666)
- Line 383: Mid-file import `from fastapi.responses import Response as _Response` -- move to top imports section
- Line 60: `from core.models.vehicle import Vehicle` -- will become unused after _build_fleet removal
- Line 59: `from core.models.route import Route, RouteAssignment` -- both unused even now (Route is never used as a class, RouteAssignment is only imported)
- Lines 14-62: Top-level imports section

From apps/kerala_delivery/config.py:
- Lines 119-122: `OSRM_URL` definition with comment block -- DELETE both the comment (lines 119-121) and the variable (line 122)

From core/geocoding/cache.py:
- Line 42: `→ SHA-256 hash for cache key (in repo layer)` -- INCORRECT. The repo layer uses `normalize_address()` (lowercase + strip + NFC) to produce `address_norm`, then does a direct btree lookup. No SHA-256 hashing occurs anywhere.

From core/geocoding/normalize.py:
- Line 8: `- GoogleGeocoder: '" ".join(address.lower().split())' then SHA-256` -- this is historical context (describing the OLD code before this module unified normalization). The reference is accurate as history but may be confusing. Leave as-is since it describes what USED to exist, or clarify with "(before this module replaced it)".

From tests/apps/kerala_delivery/api/test_api.py:
- Lines 1593, 1646: `patch("apps.kerala_delivery.api.main._build_fleet") as mock_fleet` -- must be removed from the `with` blocks. The tests mock `_build_fleet` but the upload endpoint no longer calls it (it reads from DB via `repo.get_active_vehicles`).

From tests/apps/kerala_delivery/test_config.py:
- Lines 182-188: `TestExternalServiceUrls` class with `test_osrm_url_defaults_to_localhost` -- DELETE this test. Keep `test_vroom_url_defaults_to_localhost`.

From scripts/compare_routes.py:
- Line 175: `osrm_url = args.osrm_url or config.OSRM_URL` -- change to read from env directly: `osrm_url = args.osrm_url or os.environ.get("OSRM_URL", "http://localhost:5000")`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove _build_fleet dead code and consolidate imports in main.py</name>
  <files>apps/kerala_delivery/api/main.py, tests/apps/kerala_delivery/api/test_api.py</files>
  <action>
1. **Delete `_build_fleet()` function** (lines 644-666 inclusive, including the comment block on lines 644-646). Remove the entire block from `# =============================================================================` through the closing `return vehicles`.

2. **Move mid-file import to top**: Remove line 383 (`from fastapi.responses import Response as _Response`). Add `Response` to the existing line 26 import: change `from fastapi.responses import HTMLResponse, JSONResponse` to `from fastapi.responses import HTMLResponse, JSONResponse, Response`. Then update the usage of `_Response` at line 391 to use `Response` instead (rename `_Response` to `Response` in the `serve_sw_js` function return).

3. **Remove newly-unused imports** after `_build_fleet` removal:
   - Remove `from core.models.vehicle import Vehicle` (line 60) -- only used in `_build_fleet()`
   - Remove `from core.models.route import Route, RouteAssignment` (line 59) -- `Route` was never used as a class (only appears in HTML strings), `RouteAssignment` was never referenced outside the import

4. **Update tests** in `tests/apps/kerala_delivery/api/test_api.py`:
   - In the test at ~line 1589-1593: remove the `patch("apps.kerala_delivery.api.main._build_fleet") as mock_fleet` line from the `with` block and remove the `mock_fleet.return_value = []` line (~line 1614)
   - In the test at ~line 1642-1646: same removal -- remove the `_build_fleet` mock patch line and the `mock_fleet.return_value = []` line (if present)
   - Fix the trailing backslash on the previous `with` continuation line after removing the `_build_fleet` patch (ensure the `with` block still has correct syntax with `,` and `\` on the right lines, and the last patch line ends with `:`)

**IMPORTANT**: After these changes, verify `Response` is not shadowed -- the existing `from fastapi.responses import HTMLResponse, JSONResponse` already exists, so just add `Response` to that import. The `_Response` alias was only needed because it was a mid-file import that could shadow. At top-level, `Response` is fine since nothing else uses that name.
  </action>
  <verify>
    <automated>cd /home/vishnu/projects/routing_opt && grep -n "_build_fleet" apps/kerala_delivery/api/main.py; echo "EXIT:$?"; grep -n "from fastapi.responses import Response as _Response" apps/kerala_delivery/api/main.py; echo "EXIT:$?"; grep -c "^from \|^import " apps/kerala_delivery/api/main.py; grep -n "Route\|RouteAssignment\|Vehicle" apps/kerala_delivery/api/main.py | grep "^[0-9]*:from " | head -5</automated>
  </verify>
  <done>
  - `_build_fleet()` function does not exist in main.py
  - No mid-file imports in main.py (all imports in first ~62 lines)
  - `Route`, `RouteAssignment`, `Vehicle` imports removed
  - `Response` imported at top level, `_Response` alias gone
  - Test files no longer mock `_build_fleet`
  - All tests still pass: `python3 -m pytest tests/apps/kerala_delivery/api/test_api.py -x -q`
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove OSRM_URL from config and correct stale docstrings</name>
  <files>apps/kerala_delivery/config.py, core/geocoding/cache.py, core/geocoding/normalize.py, tests/apps/kerala_delivery/test_config.py, scripts/compare_routes.py</files>
  <action>
1. **Delete `OSRM_URL` from `config.py`**: Remove lines 119-122 (the comment block and the variable definition). Keep `VROOM_URL` on what is currently line 123 (it stays). Ensure the section comment block above still makes sense -- the `# EXTERNAL SERVICES` section header (lines 112-117) should remain since `VROOM_URL` is still there, but update the descriptive comment to not reference OSRM:
   - Keep lines 112-113 (`# EXTERNAL SERVICES` / `# ===========...`)
   - Change lines 115-117 to only describe VROOM:
     ```python
     # Read from environment variables so Docker Compose networking works.
     # In Docker, services reference each other by container name (e.g., http://vroom:3000).
     # Locally, they default to localhost.
     ```
   - Remove lines 119-122 entirely (OSRM_URL comment and variable)

2. **Update `scripts/compare_routes.py`**: Change line 175 from `osrm_url = args.osrm_url or config.OSRM_URL` to `osrm_url = args.osrm_url or os.environ.get("OSRM_URL", "http://localhost:5000")`. Ensure `os` is imported at the top of the script (check -- it likely already is since config uses it).

3. **Update test**: In `tests/apps/kerala_delivery/test_config.py`, delete the `test_osrm_url_defaults_to_localhost` method (lines 185-188). If the `TestExternalServiceUrls` class only has this one test plus `test_vroom_url_defaults_to_localhost`, keep the class with just the VROOM test. If removing makes the class have only one test, that's fine.

4. **Correct `cache.py` docstring**: On line 42 of `core/geocoding/cache.py`, change:
   ```
         → SHA-256 hash for cache key (in repo layer)
   ```
   to:
   ```
         → normalize (lowercase, strip, NFC) for cache key
   ```
   This accurately describes what `normalize_address()` does. The repo layer stores the result as `address_norm` and looks it up via btree index -- no hashing.

5. **Correct `normalize.py` historical reference** (optional but recommended): On line 8 of `core/geocoding/normalize.py`, the text `GoogleGeocoder: '" ".join(address.lower().split())' then SHA-256` describes what the OLD GoogleGeocoder code used to do. This is historical context and is accurate as written. Add a clarifying note: change line 7 from `The codebase previously had three separate normalization implementations:` to `The codebase previously had three separate normalization implementations (all replaced by this module):` to make it clear this is historical.
  </action>
  <verify>
    <automated>cd /home/vishnu/projects/routing_opt && grep -n "OSRM_URL" apps/kerala_delivery/config.py; echo "---"; grep "SHA-256" core/geocoding/cache.py; echo "---"; grep "config.OSRM_URL" scripts/compare_routes.py; echo "---"; python3 -m pytest tests/apps/kerala_delivery/test_config.py -x -q 2>&1 | tail -3</automated>
  </verify>
  <done>
  - `OSRM_URL` variable does not exist in config.py
  - `scripts/compare_routes.py` reads OSRM_URL from env directly
  - `test_osrm_url_defaults_to_localhost` test removed
  - `cache.py` docstring says "normalize" not "SHA-256"
  - Config tests pass: `python3 -m pytest tests/apps/kerala_delivery/test_config.py -x -q`
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -rn "_build_fleet" apps/ core/ tests/ scripts/` returns zero matches (only .planning/ docs may reference it)
2. `grep -n "^from \|^import " apps/kerala_delivery/api/main.py` shows all imports in the first ~62 lines, none mid-file
3. `grep "OSRM_URL" apps/kerala_delivery/config.py` returns nothing
4. `grep "SHA-256" core/geocoding/cache.py` returns nothing
5. `grep "type: ignore" apps/kerala_delivery/api/main.py` still shows the PostGIS suppressions (addressed in plan 08-02)
6. `python3 -m pytest tests/apps/kerala_delivery/ -x -q` passes
</verification>

<success_criteria>
- Zero dead code: `_build_fleet()` gone, no references remain
- Zero unused imports: `Route`, `RouteAssignment`, `Vehicle` removed; all imports at file top
- Zero mid-file imports: `Response as _Response` consolidated to top
- `OSRM_URL` removed from config with all consumers updated
- Docstrings accurately describe current behavior (no SHA-256 references in cache.py)
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-api-dead-code-hygiene/08-01-SUMMARY.md`
</output>
