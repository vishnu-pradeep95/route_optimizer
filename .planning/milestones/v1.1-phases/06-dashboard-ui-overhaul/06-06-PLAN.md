---
phase: 06-dashboard-ui-overhaul
plan: 06
type: execute
wave: 3
depends_on: [06-03, 06-04, 06-05]
files_modified: []
autonomous: false
requirements: [DASH-01, DASH-02, DASH-03, DASH-04, DASH-05, DASH-06, DASH-07]

must_haves:
  truths:
    - "All 4 dashboard pages use DaisyUI components consistently with no mixed raw CSS and DaisyUI on the same page"
    - "Sidebar shows SVG icons, collapses at tablet, uses drawer on mobile"
    - "Every page has skeleton loading state and meaningful empty state"
    - "All numeric values use tabular-number alignment"
    - "Route cards show color-coded status badges"
    - "QR sheet prints cleanly with large QR codes"
  artifacts: []
  key_links: []
---

<objective>
Visual verification checkpoint for the complete Phase 06 dashboard UI overhaul.

Purpose: Human verification that all 7 DASH requirements are satisfied visually and functionally. Browser testing catches layout issues, broken interactions, and visual regressions that automated verification misses. This checkpoint covers all 4 pages, the responsive sidebar, and the QR print sheet.

Output: User confirmation that the dashboard UI overhaul meets professional logistics SaaS standards.
</objective>

<execution_context>
@/home/vishnu/.claude/get-shit-done/workflows/execute-plan.md
@/home/vishnu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-dashboard-ui-overhaul/06-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 06 Dashboard UI Overhaul:
- Plan 01: Responsive sidebar with lucide-react SVG icons + DaisyUI drawer for mobile
- Plan 02: Shared EmptyState, StatusBadge components + global .numeric utility
- Plan 03: UploadRoutes route cards (DaisyUI cards + StatusBadge) + RunHistory (DaisyUI table + skeleton/empty)
- Plan 04: LiveMap (skeleton/empty + StatsBar/VehicleList lucide icons) + FleetManagement (DaisyUI table + skeleton/empty)
- Plan 05: QR print sheet (larger QR codes, break-inside:avoid, tabular-nums, arm-length fonts)
  </what-built>
  <how-to-verify>
Start the development server:
```
cd apps/kerala_delivery/dashboard && npm run dev
```
Start the backend API:
```
cd apps/kerala_delivery && uvicorn api.main:app --reload
```

**1. Sidebar Responsiveness (DASH-02, DASH-06):**
- [ ] At >= 1280px: sidebar shows full 220px with lucide SVG icons + text labels
- [ ] At 768-1279px: sidebar collapses to 64px icon-only strip
- [ ] At < 768px: sidebar hidden, hamburger button visible in top-left
- [ ] Click hamburger: DaisyUI drawer slides in from left with full nav
- [ ] Click nav item in drawer: page changes, drawer closes
- [ ] Brand area shows Fuel icon (not gas pump emoji)
- [ ] Health dot indicator remains visible and functional at all sizes
- [ ] No emoji icons anywhere in sidebar

**2. Upload & Routes Page (DASH-01, DASH-04, DASH-05):**
- [ ] Upload a test CSV file -- drag-and-drop still works
- [ ] File icon uses lucide FileText (not emoji)
- [ ] After upload success, route cards use DaisyUI card styling
- [ ] Each card shows StatusBadge (green/amber/red based on stop statuses)
- [ ] Summary stats use DaisyUI tw-stats component
- [ ] Print QR Sheet button uses Printer icon (not emoji)
- [ ] All numbers (stops, km, min, kg) in route cards use monospace font with tabular-nums alignment
- [ ] Error state shows AlertTriangle icon (not emoji)
- [ ] Card expand/collapse for QR codes and stop list still works

**3. Run History Page (DASH-01, DASH-03, DASH-04):**
- [ ] On initial load: skeleton table visible (not spinner)
- [ ] After data loads: DaisyUI table with proper headers
- [ ] Status column shows StatusBadge component (not inline-colored pills)
- [ ] All numeric columns (orders, assigned, unassigned, vehicles, solve time) use monospace tabular-nums
- [ ] Click a row: expanded route details appear (framer-motion animation preserved)
- [ ] If no runs exist: meaningful EmptyState with ClipboardList icon

**4. Live Map Page (DASH-01, DASH-03, DASH-04):**
- [ ] On initial load: skeleton that matches 3-panel layout (stats bar + vehicle list + map placeholder)
- [ ] After data loads: StatsBar values use monospace tabular-nums
- [ ] VehicleList uses lucide icons (Package, Ruler, Scale) not emoji
- [ ] VehicleList numeric values (stops, km, kg, km/delivery) use tabular-nums
- [ ] RouteMap theme toggle uses Moon/Sun icons (not emoji)
- [ ] Vehicle selection still zooms the map
- [ ] Telemetry polling still refreshes positions
- [ ] If no routes: EmptyState with MapPin icon

**5. Fleet Management Page (DASH-01, DASH-03, DASH-04):**
- [ ] On initial load: skeleton table visible
- [ ] After data loads: DaisyUI table with proper styling
- [ ] Numeric columns (weight, items, speed limit) use tabular-nums
- [ ] Edit button uses Pencil icon (not emoji)
- [ ] Active/Inactive status shows DaisyUI badge
- [ ] Add Vehicle form works -- inputs use DaisyUI styling
- [ ] Edit inline works -- save/cancel still functional
- [ ] Deactivate/Reactivate works
- [ ] If no vehicles: EmptyState with Truck icon + "Add Vehicle" button

**6. QR Print Sheet (DASH-07):**
- [ ] Open http://localhost:8000/api/qr-sheet in browser
- [ ] QR codes are visibly larger (~210px, not the old 150px)
- [ ] Card stats show vehicle ID, driver name, stops, distance, duration, weight
- [ ] Numbers in card stats are tabular-aligned
- [ ] Vehicle ID font is large and bold (~22px)
- [ ] Open Chrome print preview (Ctrl+P): no card is split across page boundaries
- [ ] Print preview shows professional, clean layout on A4

**7. Cross-Page Consistency (DASH-01):**
- [ ] All 4 pages feel like they belong to the same application
- [ ] No page has mixed raw CSS buttons alongside DaisyUI styled buttons
- [ ] Color palette is consistent (amber accent, stone neutrals, green/red status)
- [ ] Typography is consistent (DM Sans headings, IBM Plex Mono numbers)
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe specific issues to fix (e.g., "LiveMap skeleton doesn't match layout" or "FleetManagement edit form styling broken")</resume-signal>
</task>

</tasks>

<verification>
All verification is manual for this checkpoint plan. The automated checks were completed in Plans 01-05.
</verification>

<success_criteria>
User confirms all 7 DASH requirements are visually and functionally satisfied across all 4 dashboard pages, the responsive sidebar, and the QR print sheet.
</success_criteria>

<output>
After completion, create `.planning/phases/06-dashboard-ui-overhaul/06-06-SUMMARY.md`
</output>
