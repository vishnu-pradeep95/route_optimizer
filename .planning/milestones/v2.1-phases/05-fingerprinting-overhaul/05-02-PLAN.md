---
phase: 05-fingerprinting-overhaul
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - docker-compose.yml
  - docker-compose.license-test.yml
autonomous: false
requirements: [FPR-01, FPR-02]

must_haves:
  truths:
    - "Docker API container can read /etc/machine-id from the host"
    - "Host and container produce the same fingerprint"
    - "Rebuilding or recreating the API container does not change the fingerprint"
    - "License E2E test container also has /etc/machine-id mounted"
  artifacts:
    - path: "docker-compose.yml"
      provides: "/etc/machine-id bind mount on api service"
      contains: "/etc/machine-id:/etc/machine-id:ro"
    - path: "docker-compose.license-test.yml"
      provides: "/etc/machine-id bind mount on api-license-test service"
      contains: "/etc/machine-id:/etc/machine-id:ro"
  key_links:
    - from: "docker-compose.yml"
      to: "core/licensing/license_manager.py"
      via: "bind mount enables _read_machine_id() to read host identity inside container"
      pattern: "/etc/machine-id:/etc/machine-id:ro"
    - from: "docker-compose.license-test.yml"
      to: "core/licensing/license_manager.py"
      via: "bind mount for license E2E test container"
      pattern: "/etc/machine-id:/etc/machine-id:ro"
---

<objective>
Add /etc/machine-id bind mounts to Docker Compose files and verify that host and container produce identical fingerprints.

Purpose: The new fingerprint formula reads /etc/machine-id, but the python:3.12-slim Docker image has no /etc/machine-id. Without the bind mount, the container falls back to an empty string, producing a different fingerprint than the host.

Output: Updated docker-compose files with bind mounts, verified host-container fingerprint consistency.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v2.1-phases/05-fingerprinting-overhaul/05-RESEARCH.md
@.planning/milestones/v2.1-phases/05-fingerprinting-overhaul/05-01-SUMMARY.md

@docker-compose.yml
@docker-compose.license-test.yml

<interfaces>
<!-- api service volumes in docker-compose.yml (lines 220-222) -->
```yaml
  api:
    volumes:
      - ./data:/app/data
      - dashboard_assets:/srv/dashboard:ro
```

<!-- api-license-test service in docker-compose.license-test.yml (entire file) -->
```yaml
services:
  api-license-test:
    build:
      context: .
      dockerfile: infra/Dockerfile
    ports:
      - "8001:8000"
    environment:
      - ENVIRONMENT=production
      - LICENSE_KEY=invalid-test-key-for-e2e
      # ... other env vars
    depends_on:
      db:
        condition: service_healthy
```
<!-- Note: api-license-test has NO volumes section currently -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add /etc/machine-id bind mount to Docker Compose files</name>
  <files>docker-compose.yml, docker-compose.license-test.yml</files>
  <action>
**In docker-compose.yml:**

Add `/etc/machine-id:/etc/machine-id:ro` to the api service volumes (after line 222, the existing volumes):

```yaml
    volumes:
      - ./data:/app/data
      - dashboard_assets:/srv/dashboard:ro
      - /etc/machine-id:/etc/machine-id:ro   # Machine identity for license fingerprint
```

**In docker-compose.license-test.yml:**

Add a `volumes` section to the api-license-test service (it currently has none):

```yaml
  api-license-test:
    build:
      context: .
      dockerfile: infra/Dockerfile
    ports:
      - "8001:8000"
    volumes:
      - /etc/machine-id:/etc/machine-id:ro   # Machine identity for license fingerprint
    environment:
      # ... existing env vars unchanged
```

After editing, validate compose files parse correctly:
`docker compose -f docker-compose.yml config --quiet`
`docker compose -f docker-compose.yml -f docker-compose.license-test.yml config --quiet`

Then rebuild and restart the API container to pick up the new mount:
`docker compose build api && docker compose up -d --no-deps api`
  </action>
  <verify>
    <automated>docker compose -f docker-compose.yml config --quiet && docker compose -f docker-compose.yml -f docker-compose.license-test.yml config --quiet && docker exec lpg-api cat /etc/machine-id</automated>
  </verify>
  <done>Both compose files parse without errors. The api container has /etc/machine-id mounted and readable. The file content matches the host's /etc/machine-id.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify fingerprint stability across host, container, and recreate</name>
  <what-built>
New fingerprint formula (machine-id + CPU model) implemented in Plan 01, Docker bind mounts added in Task 1. The fingerprint should now be identical between host and container, and stable across container recreation.
  </what-built>
  <how-to-verify>
Run these three checks and compare results:

1. **Host fingerprint:**
   ```
   python3 scripts/get_machine_id.py
   ```
   Note the fingerprint value.

2. **Container fingerprint:**
   ```
   docker exec lpg-api python3 scripts/get_machine_id.py
   ```
   Should show the SAME fingerprint as step 1.

3. **Stability across recreate:**
   ```
   docker compose up -d --force-recreate api
   docker exec lpg-api python3 scripts/get_machine_id.py
   ```
   Should show the SAME fingerprint as steps 1 and 2.

Expected: All three commands produce identical fingerprint values.

If any fingerprint differs, check:
- `docker exec lpg-api cat /etc/machine-id` matches `cat /etc/machine-id`
- `docker exec lpg-api grep "model name" /proc/cpuinfo | head -1` matches host output
  </how-to-verify>
  <resume-signal>Type "approved" if all three fingerprints match, or describe the mismatch</resume-signal>
</task>

</tasks>

<verification>
1. `docker compose -f docker-compose.yml config --quiet` -- compose file valid
2. `docker exec lpg-api cat /etc/machine-id` -- returns host machine-id
3. `python3 scripts/get_machine_id.py` vs `docker exec lpg-api python3 scripts/get_machine_id.py` -- identical output
4. `grep "/etc/machine-id:/etc/machine-id:ro" docker-compose.yml` -- bind mount present
5. `grep "/etc/machine-id:/etc/machine-id:ro" docker-compose.license-test.yml` -- bind mount present
</verification>

<success_criteria>
- /etc/machine-id is accessible inside the API container via read-only bind mount
- Host and container produce the same fingerprint (success criterion #2 from roadmap)
- Fingerprint is stable across container recreate (success criterion #3 from roadmap)
- License E2E test container also has the bind mount configured
- Full test suite still passes: `python3 -m pytest tests/ -x -q`
</success_criteria>

<output>
After completion, create `.planning/milestones/v2.1-phases/05-fingerprinting-overhaul/05-02-SUMMARY.md`
</output>
