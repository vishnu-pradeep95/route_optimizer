---
phase: 16-driver-database-foundation
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - apps/kerala_delivery/api/main.py
  - apps/kerala_delivery/api/errors.py
  - apps/kerala_delivery/dashboard/src/pages/DriverManagement.tsx
  - apps/kerala_delivery/dashboard/src/pages/DriverManagement.css
  - apps/kerala_delivery/dashboard/src/lib/api.ts
  - apps/kerala_delivery/dashboard/src/types.ts
  - apps/kerala_delivery/dashboard/src/App.tsx
  - tests/apps/kerala_delivery/api/test_api.py
autonomous: true
requirements: [DRV-01, DRV-02, DRV-03, DRV-04]

must_haves:
  truths:
    - "User can open the Drivers page from the sidebar and see a list of all drivers with name, active/inactive badge, and route count"
    - "User can add a new driver by name using an inline form, with a fuzzy-match warning if a similar driver exists"
    - "User can edit an existing driver's name inline, with a fuzzy-match warning on the new name"
    - "User can deactivate and reactivate a driver from the table"
    - "GET /api/drivers returns all drivers with route counts"
    - "POST /api/drivers creates a driver and returns fuzzy match info"
    - "PUT /api/drivers/{id} updates driver name"
    - "DELETE /api/drivers/{id} soft-deactivates a driver"
    - "GET /api/drivers/check-name returns similar matches for a given name"
  artifacts:
    - path: "apps/kerala_delivery/api/main.py"
      provides: "5 driver API endpoints (GET list, GET check-name, POST, PUT, DELETE)"
      contains: "/api/drivers"
    - path: "apps/kerala_delivery/dashboard/src/pages/DriverManagement.tsx"
      provides: "Driver management page with table, inline add/edit, fuzzy warning"
      min_lines: 200
    - path: "apps/kerala_delivery/dashboard/src/types.ts"
      provides: "Driver, DriversResponse, DriverCheckResponse types"
      contains: "interface Driver"
    - path: "apps/kerala_delivery/dashboard/src/lib/api.ts"
      provides: "fetchDrivers, createDriver, updateDriver, deleteDriver, checkDriverName functions"
      contains: "fetchDrivers"
    - path: "apps/kerala_delivery/dashboard/src/App.tsx"
      provides: "Sidebar with Drivers link replacing Fleet, DriverManagement page rendering"
      contains: "DriverManagement"
  key_links:
    - from: "apps/kerala_delivery/dashboard/src/pages/DriverManagement.tsx"
      to: "/api/drivers"
      via: "fetchDrivers in useEffect"
      pattern: "fetchDrivers"
    - from: "apps/kerala_delivery/dashboard/src/pages/DriverManagement.tsx"
      to: "/api/drivers/check-name"
      via: "checkDriverName on name input blur"
      pattern: "checkDriverName"
    - from: "apps/kerala_delivery/api/main.py"
      to: "core/database/repository.py"
      via: "repository driver CRUD calls"
      pattern: "repository\\.(get_all_drivers|create_driver|update_driver|deactivate_driver|find_similar_drivers)"
---

<objective>
Build the 5 driver API endpoints (list, check-name, create, update, deactivate), the DriverManagement dashboard page (replacing FleetManagement), and wire everything together -- sidebar nav, types, API client functions.

Purpose: Users need a way to view, add, edit, and deactivate drivers through the dashboard. The API endpoints serve both the dashboard and will be used by the CSV auto-creation flow (Plan 03).
Output: Working driver CRUD flow from dashboard to database, with fuzzy name matching warnings on add/edit.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/milestones/v3.0-phases/16-driver-database-foundation/16-CONTEXT.md
@.planning/milestones/v3.0-phases/16-driver-database-foundation/16-RESEARCH.md
@.planning/milestones/v3.0-phases/16-driver-database-foundation/16-01-SUMMARY.md

@apps/kerala_delivery/api/main.py
@apps/kerala_delivery/api/errors.py
@apps/kerala_delivery/dashboard/src/pages/FleetManagement.tsx
@apps/kerala_delivery/dashboard/src/pages/FleetManagement.css
@apps/kerala_delivery/dashboard/src/lib/api.ts
@apps/kerala_delivery/dashboard/src/types.ts
@apps/kerala_delivery/dashboard/src/App.tsx
@tests/apps/kerala_delivery/api/test_api.py

<interfaces>
<!-- From Plan 01 (repository.py) — driver CRUD functions to call from API endpoints -->
async def get_all_drivers(session: AsyncSession, active_only: bool = False) -> list[DriverDB]
async def get_driver_by_id(session: AsyncSession, driver_id: uuid.UUID) -> DriverDB | None
async def create_driver(session: AsyncSession, name: str) -> DriverDB
async def update_driver_name(session: AsyncSession, driver_id: uuid.UUID, new_name: str) -> bool
async def deactivate_driver(session: AsyncSession, driver_id: uuid.UUID) -> bool
async def reactivate_driver(session: AsyncSession, driver_id: uuid.UUID) -> bool
async def find_similar_drivers(session: AsyncSession, name: str, exclude_id: uuid.UUID | None = None) -> list[tuple[DriverDB, float]]
async def get_driver_route_counts(session: AsyncSession) -> dict[uuid.UUID, int]
def normalize_driver_name(name: str) -> str

<!-- Existing vehicle API endpoint pattern (main.py:2369-2515) -->
@app.get("/api/vehicles", dependencies=[Depends(verify_read_key)])
@app.post("/api/vehicles", dependencies=[Depends(verify_api_key)])
@app.put("/api/vehicles/{vehicle_id}", dependencies=[Depends(verify_api_key)])
@app.delete("/api/vehicles/{vehicle_id}", dependencies=[Depends(verify_api_key)])

<!-- Existing vehicle API client pattern (lib/api.ts:159-189) -->
export async function fetchVehicles(activeOnly: boolean = false): Promise<VehiclesResponse>
export async function createVehicle(data: {...}): Promise<{ message: string; vehicle: Vehicle }>
export async function updateVehicle(vehicleId: string, data: Record<string, unknown>): Promise<{ message: string }>
export async function deleteVehicle(vehicleId: string): Promise<{ message: string }>

<!-- Existing error codes pattern (errors.py:47-51) -->
FLEET_NO_VEHICLES = "FLEET_NO_VEHICLES"
FLEET_VEHICLE_NOT_FOUND = "FLEET_VEHICLE_NOT_FOUND"
FLEET_VEHICLE_EXISTS = "FLEET_VEHICLE_EXISTS"
FLEET_NO_FIELDS = "FLEET_NO_FIELDS"

<!-- App.tsx sidebar pattern (lines 52-56) -->
type Page = "upload" | "live-map" | "run-history" | "fleet";
const NAV_ITEMS: NavItem[] = [
  { page: "fleet", icon: Truck, label: "Fleet" },
];

<!-- Dashboard component imports used in FleetManagement -->
import { EmptyState } from "../components/EmptyState";
import { ErrorBanner } from "../components/ErrorBanner";
import { isApiError } from "../lib/errors";
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Driver API endpoints + error codes + API tests</name>
  <files>apps/kerala_delivery/api/main.py, apps/kerala_delivery/api/errors.py, tests/apps/kerala_delivery/api/test_api.py</files>
  <behavior>
    - Test: GET /api/drivers returns 200 with {count, drivers} where each driver has id, name, is_active, route_count, created_at, updated_at
    - Test: GET /api/drivers?active_only=true returns only active drivers
    - Test: GET /api/drivers/check-name?name=SURESH returns {similar_drivers: [{id, name, score, is_active}]}
    - Test: POST /api/drivers with {name: "suresh kumar"} returns 201 with created driver (title-cased) and similar_drivers field
    - Test: POST /api/drivers with empty name returns 400
    - Test: PUT /api/drivers/{id} with {name: "new name"} returns 200, updates name
    - Test: PUT /api/drivers/{id} on non-existent returns 404
    - Test: DELETE /api/drivers/{id} returns 200, driver becomes inactive
    - Test: DELETE /api/drivers/{id} on non-existent returns 404
    - Test: POST /api/drivers requires API key (no key returns 401)
  </behavior>
  <action>
    1. **Add error codes** to `apps/kerala_delivery/api/errors.py`:
       - `DRIVER_NOT_FOUND = "DRIVER_NOT_FOUND"`
       - `DRIVER_NAME_EMPTY = "DRIVER_NAME_EMPTY"`
       - `DRIVER_NAME_DUPLICATE = "DRIVER_NAME_DUPLICATE"` (for exact name_normalized match)

    2. **Add 5 endpoints** to `apps/kerala_delivery/api/main.py` (after vehicle endpoints, around line 2520), following the vehicle endpoint pattern:

       **GET /api/drivers** (dependencies=[Depends(verify_read_key)]):
       - Query param: `active_only: bool = False`
       - Calls `repository.get_all_drivers(session, active_only)` and `repository.get_driver_route_counts(session)`
       - Returns `{"count": N, "drivers": [{id, name, is_active, route_count, created_at, updated_at}, ...]}`
       - Helper function `_driver_to_dict(driver, route_count)` for serialization

       **GET /api/drivers/check-name** (dependencies=[Depends(verify_read_key)]):
       - Query params: `name: str`, `exclude_id: str | None = None`
       - Calls `repository.find_similar_drivers(session, name, exclude_id)`
       - Returns `{"similar_drivers": [{id, name, score, is_active}, ...]}`
       - IMPORTANT: Place this route BEFORE the `/{id}` routes so FastAPI doesn't try to parse "check-name" as a UUID

       **POST /api/drivers** (dependencies=[Depends(verify_api_key)], rate limit 20/min):
       - Body: Pydantic model `DriverCreate` with `name: str` (min_length=1, max_length=100)
       - Call `repository.find_similar_drivers(session, name)` to get matches
       - Call `repository.create_driver(session, name)`, commit
       - Return 201 with `{"message": "Driver created", "driver": {...}, "similar_drivers": [...]}`
       - The similar_drivers list is informational -- auto-merge only happens during CSV upload, not manual creation

       **PUT /api/drivers/{id}** (dependencies=[Depends(verify_api_key)], rate limit 20/min):
       - Path param: `id: str` (UUID as string)
       - Body: Pydantic model `DriverUpdate` with `name: str | None`, `is_active: bool | None`
       - If name provided: call `repository.update_driver_name(session, driver_id, name)`, commit
       - If is_active provided: call reactivate or deactivate accordingly
       - Return 200 with `{"message": "Driver updated", "similar_drivers": [...]}` (similar_drivers only if name changed)
       - Return 404 with DRIVER_NOT_FOUND if not found

       **DELETE /api/drivers/{id}** (dependencies=[Depends(verify_api_key)], rate limit 10/min):
       - Calls `repository.deactivate_driver(session, driver_id)`, commit
       - Return 200 with `{"message": "Driver deactivated"}`
       - Return 404 with DRIVER_NOT_FOUND if not found

    3. **Add API tests** to `tests/apps/kerala_delivery/api/test_api.py`:
       - Mock repository functions using the same pattern as existing vehicle tests
       - Test each endpoint for success and error cases
       - Test auth requirement on write endpoints (POST, PUT, DELETE)
  </action>
  <verify>
    <automated>cd /home/vishnu/projects/routing_opt && source .venv/bin/activate && pytest tests/apps/kerala_delivery/api/test_api.py -k "driver" -x -v</automated>
  </verify>
  <done>5 driver API endpoints working with proper auth, rate limiting, error codes. All new driver API tests pass. check-name endpoint returns fuzzy matches. Create/update responses include similar_drivers for dashboard warnings.</done>
</task>

<task type="auto">
  <name>Task 2: DriverManagement dashboard page + types + API client + sidebar wiring</name>
  <files>apps/kerala_delivery/dashboard/src/pages/DriverManagement.tsx, apps/kerala_delivery/dashboard/src/pages/DriverManagement.css, apps/kerala_delivery/dashboard/src/lib/api.ts, apps/kerala_delivery/dashboard/src/types.ts, apps/kerala_delivery/dashboard/src/App.tsx</files>
  <action>
    1. **Add TypeScript types** to `apps/kerala_delivery/dashboard/src/types.ts`:
       ```typescript
       export interface Driver {
         id: string;
         name: string;
         is_active: boolean;
         route_count: number;
         created_at: string | null;
         updated_at: string | null;
       }

       export interface DriversResponse {
         count: number;
         drivers: Driver[];
       }

       export interface DriverCheckResponse {
         similar_drivers: Array<{
           id: string;
           name: string;
           score: number;
           is_active: boolean;
         }>;
       }
       ```

    2. **Add API client functions** to `apps/kerala_delivery/dashboard/src/lib/api.ts` (after the vehicle functions):
       - `fetchDrivers(activeOnly?: boolean): Promise<DriversResponse>` -- GET /api/drivers
       - `createDriver(name: string): Promise<{ message: string; driver: Driver; similar_drivers: Array<{id: string; name: string; score: number; is_active: boolean}> }>` -- POST /api/drivers
       - `updateDriver(id: string, data: { name?: string; is_active?: boolean }): Promise<{ message: string; similar_drivers?: Array<{id: string; name: string; score: number; is_active: boolean}> }>` -- PUT /api/drivers/{id}
       - `deleteDriver(id: string): Promise<{ message: string }>` -- DELETE /api/drivers/{id}
       - `checkDriverName(name: string, excludeId?: string): Promise<DriverCheckResponse>` -- GET /api/drivers/check-name?name=X&exclude_id=Y

    3. **Create DriverManagement.tsx** following FleetManagement.tsx inline CRUD pattern but simpler (only 1 field: name):
       - **State**: drivers list, loading, error, editingId, addingNew, newDriverName, editDriverName, similarWarnings
       - **On mount**: fetch drivers list via fetchDrivers()
       - **Table**: Columns: Name, Status (DaisyUI badge -- `tw:badge tw:badge-success` for active, `tw:badge tw:badge-ghost` for inactive), Route Count, Actions
       - **Add form** (shown when addingNew is true): Single inline text input for name + save/cancel buttons. On blur or before save, call checkDriverName() and show fuzzy match warning banner if similar drivers found (amber alert with list of similar names and scores). User can still proceed with create despite warning.
       - **Edit row**: When editingId matches, row switches to an inline text input. Same fuzzy check on blur with warning. Save calls updateDriver(), cancel reverts.
       - **Actions column**: Edit (Pencil icon), Deactivate/Reactivate toggle button
       - **Empty state**: Use `<EmptyState>` component with Users icon, "No drivers yet", "Drivers are automatically created when you upload a CDCMS file, or you can add them manually.", action button "Add Driver"
       - **Error handling**: Use `<ErrorBanner>` with retry (re-fetch) and dismiss
       - **Fuzzy warning banner**: When checkDriverName returns matches, show an amber DaisyUI alert below the input: "Similar driver(s) found: {name} ({score}% match, {active/inactive}). Continue to create a new driver, or cancel."
       - Use all `tw:` prefix classes per project convention
       - Import icons from lucide-react: Users, UserPlus, Pencil, RotateCw, X, Check, Plus

    4. **Create DriverManagement.css** based on FleetManagement.css pattern. Simpler layout since there's only one field (name) instead of an 8-field grid.

    5. **Update App.tsx**:
       - Replace `import { FleetManagement }` with `import { DriverManagement }` from `"./pages/DriverManagement"`
       - Update Page type: change `"fleet"` to `"drivers"` (or keep as "fleet" for URL stability -- use Claude's discretion, but update the label)
       - Update NAV_ITEMS: change `{ page: "fleet", icon: Truck, label: "Fleet" }` to `{ page: "drivers", icon: Users, label: "Drivers" }`
       - Sidebar order per locked decision: Upload Routes, Live Map, Run History, Drivers
       - Update page rendering: replace `{activePage === "fleet" && <FleetManagement />}` with `{activePage === "drivers" && <DriverManagement />}`
       - Import Users from lucide-react, remove Truck if no longer used
  </action>
  <verify>
    <automated>cd /home/vishnu/projects/routing_opt/apps/kerala_delivery/dashboard && npm run build</automated>
  </verify>
  <done>DriverManagement page renders with driver table (name, status badge, route count, actions). Inline add form with fuzzy match warning works. Inline edit with fuzzy match warning works. Deactivate/reactivate toggles work. Sidebar shows "Drivers" with Users icon in correct position. Dashboard builds without errors. FleetManagement is replaced.</done>
</task>

</tasks>

<verification>
- `pytest tests/apps/kerala_delivery/api/test_api.py -k "driver" -x -v` -- all driver API tests pass
- `cd apps/kerala_delivery/dashboard && npm run build` -- dashboard builds without errors
- `grep -c "DriverManagement" apps/kerala_delivery/dashboard/src/App.tsx` returns >= 1
- `grep -c "FleetManagement" apps/kerala_delivery/dashboard/src/App.tsx` returns 0
- `grep -c "fetchDrivers" apps/kerala_delivery/dashboard/src/lib/api.ts` returns >= 1
- `grep -c "interface Driver" apps/kerala_delivery/dashboard/src/types.ts` returns >= 1
- `grep -c "/api/drivers" apps/kerala_delivery/api/main.py` returns >= 5
</verification>

<success_criteria>
- All 5 driver API endpoints respond correctly (list, check-name, create, update, deactivate)
- DriverManagement page replaces FleetManagement in sidebar and renders driver table
- Fuzzy name match warnings display on add/edit in the dashboard
- Dashboard builds and all API tests pass
- Sidebar shows "Drivers" with Users icon (not "Fleet" with Truck icon)
</success_criteria>

<output>
After completion, create `.planning/milestones/v3.0-phases/16-driver-database-foundation/16-02-SUMMARY.md`
</output>
