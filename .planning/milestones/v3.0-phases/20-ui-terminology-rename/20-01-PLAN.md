---
phase: 20-ui-terminology-rename
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/kerala_delivery/dashboard/src/components/VehicleList.tsx
  - apps/kerala_delivery/dashboard/src/components/VehicleList.css
  - apps/kerala_delivery/dashboard/src/components/RouteList.tsx
  - apps/kerala_delivery/dashboard/src/components/RouteList.css
  - apps/kerala_delivery/dashboard/src/pages/LiveMap.tsx
  - apps/kerala_delivery/dashboard/src/pages/UploadRoutes.tsx
  - apps/kerala_delivery/dashboard/src/pages/RunHistory.tsx
  - apps/kerala_delivery/dashboard/src/components/StatsBar.tsx
  - apps/kerala_delivery/dashboard/src/pages/FleetManagement.tsx
  - apps/kerala_delivery/dashboard/src/pages/FleetManagement.css
autonomous: true
requirements:
  - UI-01
  - UI-03

must_haves:
  truths:
    - "Dashboard sidebar heading says 'Routes' instead of 'Vehicles'"
    - "StatsBar shows 'Drivers Active' instead of 'Vehicles Active'"
    - "Upload results summary shows 'Drivers' instead of 'Vehicles'"
    - "Run History table headers show 'Drivers' and 'Driver' instead of 'Vehicles' and 'Vehicle'"
    - "Route cards show driver names as primary identifiers"
    - "FleetManagement page is gone -- no way to navigate to it"
  artifacts:
    - path: "apps/kerala_delivery/dashboard/src/components/RouteList.tsx"
      provides: "Renamed VehicleList component with all user-visible text updated"
      exports: ["RouteList"]
    - path: "apps/kerala_delivery/dashboard/src/components/RouteList.css"
      provides: "Renamed CSS with .route-* class names"
      contains: ".route-list"
  key_links:
    - from: "apps/kerala_delivery/dashboard/src/pages/LiveMap.tsx"
      to: "apps/kerala_delivery/dashboard/src/components/RouteList.tsx"
      via: "import { RouteList }"
      pattern: "import.*RouteList.*from.*RouteList"
---

<objective>
Rename VehicleList to RouteList (file + all CSS classes), sweep all user-visible "Vehicle" text to "Driver" across the entire dashboard, and delete the dead FleetManagement page.

Purpose: The system now operates on a driver-centric model (Phase 16-19). The dashboard UI must reflect this terminology so office staff see "Driver" everywhere, not "Vehicle".
Output: RouteList.tsx/css (renamed from VehicleList), updated text in StatsBar/UploadRoutes/RunHistory, FleetManagement.tsx/css deleted.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v3.0-phases/20-ui-terminology-rename/20-CONTEXT.md
@.planning/milestones/v3.0-phases/20-ui-terminology-rename/20-RESEARCH.md

@apps/kerala_delivery/dashboard/src/components/VehicleList.tsx
@apps/kerala_delivery/dashboard/src/components/VehicleList.css
@apps/kerala_delivery/dashboard/src/pages/LiveMap.tsx
@apps/kerala_delivery/dashboard/src/App.tsx

<interfaces>
<!-- Key types the executor needs. Extracted from codebase. -->

From types.ts (DO NOT MODIFY -- API contract):
```typescript
export interface RouteSummary {
  route_id: string;
  vehicle_id: string;      // KEEP -- API field name, backward-compatible
  driver_name: string;
  total_stops: number;
  total_distance_km: number;
  total_duration_minutes: number;
  total_weight_kg: number;
  total_items: number;
}
```

From LiveMap.tsx (current imports to update):
```typescript
import { VehicleList } from "../components/VehicleList";
// ...
<VehicleList
  routes={routes}
  routeDetailsMap={routeDetailsMap}
  latestPings={latestPings}
  selectedVehicleId={selectedVehicleId}
  onSelectVehicle={handleSelectVehicle}
  vehicleIndexMap={vehicleIndexMap}
/>
```

From App.tsx (already correct -- no FleetManagement import):
```typescript
const NAV_ITEMS = [
  { page: "upload", icon: Upload, label: "Upload & Routes" },
  { page: "live-map", icon: Map, label: "Live Map" },
  { page: "run-history", icon: ClipboardList, label: "Run History" },
  { page: "drivers", icon: Users, label: "Drivers" },
];
```
App.tsx does NOT import FleetManagement -- it was already removed in Phase 16.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename VehicleList to RouteList (file + CSS classes + imports)</name>
  <files>
    apps/kerala_delivery/dashboard/src/components/RouteList.tsx,
    apps/kerala_delivery/dashboard/src/components/RouteList.css,
    apps/kerala_delivery/dashboard/src/pages/LiveMap.tsx
  </files>
  <action>
1. Create RouteList.tsx from VehicleList.tsx with these changes:
   - Rename interface `VehicleListProps` to `RouteListProps`
   - Rename export function `VehicleList` to `RouteList`
   - Change import from `"./VehicleList.css"` to `"./RouteList.css"`
   - Change JSX class names from `.vehicle-*` to `.route-*`:
     - `vehicle-list` -> `route-list`
     - `vehicle-list-header` -> `route-list-header`
     - `vehicle-list-items` -> `route-list-items`
     - `vehicle-item` -> `route-item`
     - `vehicle-item-header` -> `route-item-header`
     - `vehicle-color-dot` -> `route-color-dot`
     - `vehicle-id` -> `route-id`
     - `vehicle-driver` -> `route-driver`
     - `vehicle-stats` -> `route-stats`
     - `vehicle-stat` -> `route-stat`
     - `vehicle-efficiency` -> `route-efficiency`
     - `vehicle-eta` -> `route-eta`
     - `vehicle-progress` -> `route-progress`
     - `vehicle-progress-bar` -> `route-progress-bar`
     - `vehicle-list-empty` -> `route-list-empty`
   - Change user-visible text:
     - `<h3>Vehicles</h3>` -> `<h3>Routes</h3>`
     - `title="Show all vehicles"` -> `title="Show all routes"`
   - Keep all prop names that reference `vehicle_id` (API field), `vehicleIndexMap`, `selectedVehicleId`, `onSelectVehicle` -- these are internal code, not user-visible. The props interface field names can stay for backward compatibility with LiveMap.tsx.
   - Update file-level docstring: "VehicleList" -> "RouteList", "vehicles" -> "routes" in description.

2. Create RouteList.css from VehicleList.css with these changes:
   - Rename ALL CSS selectors from `.vehicle-*` to `.route-*` (18 selectors)
   - Update the `@keyframes vehicle-pulse` to `@keyframes route-pulse`
   - Update the animation reference in `.speed-alert-badge`
   - Update the file-level CSS comment

3. Delete VehicleList.tsx and VehicleList.css (git rm).

4. Update LiveMap.tsx:
   - Change import: `import { VehicleList }` -> `import { RouteList }` from `"../components/RouteList"`
   - Change JSX: `<VehicleList` -> `<RouteList` (the props stay the same)
  </action>
  <verify>
    <automated>cd /home/vishnu/projects/routing_opt/apps/kerala_delivery/dashboard && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    RouteList.tsx/css exist with .route-* class names. VehicleList.tsx/css deleted. LiveMap.tsx imports RouteList. TypeScript compiles with no errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sweep Vehicle-to-Driver text + delete FleetManagement</name>
  <files>
    apps/kerala_delivery/dashboard/src/components/StatsBar.tsx,
    apps/kerala_delivery/dashboard/src/pages/UploadRoutes.tsx,
    apps/kerala_delivery/dashboard/src/pages/RunHistory.tsx,
    apps/kerala_delivery/dashboard/src/pages/FleetManagement.tsx,
    apps/kerala_delivery/dashboard/src/pages/FleetManagement.css
  </files>
  <action>
1. StatsBar.tsx:
   - Line 100: Change `"Vehicles Active"` to `"Drivers Active"`
   - Line 22: Update comment "any vehicle" to "any driver" (optional but clean)

2. UploadRoutes.tsx -- user-visible text changes:
   - Line 7 (comment): "per vehicle" -> "per driver"
   - Line 12 (comment): "vehicle/driver details" -> "driver details"
   - Line 856: `"Vehicles"` stat title -> `"Drivers"`
   - Line 877: `"Vehicles"` stat title -> `"Drivers"`
   - Line 907 (comment): `{/* Vehicle Route Cards */}` -> `{/* Driver Route Cards */}`
   - Line 925: The badge shows `route.vehicle_id` which now contains driver names per Phase 19 -- this is correct as-is. But update the route card header to make driver name primary:
     - Change from: `<span className="tw:badge ...">{route.vehicle_id}</span> <span className="text-muted-60">{route.driver_name}</span>`
     - Change to: `<span className="tw:font-semibold">{route.vehicle_id}</span>` (vehicle_id now IS the driver name; driver_name is redundant. Show just one, the vehicle_id which is the driver name)
   - Rename state variable `expandedVehicle` to `expandedRoute` for clarity (lines 313, 561, 569, 570, 912)
   - Rename function `toggleVehicle` to `toggleRoute` for clarity (lines 569, 922)
   - Rename variable `vehicleQr` to `routeQr` for clarity in the route cards section

3. RunHistory.tsx:
   - Line 133: `<th>Vehicles</th>` -> `<th>Drivers</th>`
   - Line 289: `<th>Vehicle</th>` -> `<th>Driver</th>`

4. Delete FleetManagement.tsx and FleetManagement.css:
   - `git rm` both files
   - Verify App.tsx does NOT import FleetManagement (confirmed: it doesn't)
   - Clean the stale comment in DriverManagement.css line 5 that references FleetManagement

5. Update browser tab title in index.html (if applicable) or App.tsx:
   - Set document title to "Kerala LPG Ops" or similar driver-centric title. Check `apps/kerala_delivery/dashboard/index.html` for the `<title>` tag and update if it says "Vehicle" or "Fleet".
  </action>
  <verify>
    <automated>cd /home/vishnu/projects/routing_opt/apps/kerala_delivery/dashboard && npx tsc --noEmit 2>&1 | head -30 && grep -rn "Vehicles" src/ --include="*.tsx" | grep -v "vehicle_id\|vehicleId\|vehicleIndex\|VEHICLE_COLORS\|getVehicleColor\|vehicles_used\|VehiclesResponse\|Vehicle interface\|fetchVehicles\|node_modules" | head -20</automated>
  </verify>
  <done>
    TypeScript compiles with no errors. No user-visible "Vehicles" text remains in dashboard (only API field names like vehicle_id preserved). FleetManagement.tsx/css deleted. StatsBar shows "Drivers Active". UploadRoutes shows "Drivers" in stats. RunHistory shows "Drivers"/"Driver" in table headers.
  </done>
</task>

</tasks>

<verification>
1. `cd apps/kerala_delivery/dashboard && npx tsc --noEmit` -- zero TypeScript errors
2. `cd apps/kerala_delivery/dashboard && npm run build` -- production build succeeds
3. Grep for user-visible "Vehicle" text: `grep -rn '"Vehicle' src/ --include="*.tsx"` returns nothing (only internal code references)
4. Grep confirms RouteList exists and VehicleList files deleted
5. FleetManagement.tsx/css no longer exist
</verification>

<success_criteria>
- All user-visible dashboard text says "Driver" or "Routes" instead of "Vehicle" or "Vehicles"
- VehicleList.tsx/css renamed to RouteList.tsx/css with .route-* CSS classes
- FleetManagement.tsx/css deleted (DriverManagement.tsx is the only management page)
- LiveMap.tsx imports and uses RouteList component
- App.tsx navigation unchanged (already correct from Phase 16)
- API field names (vehicle_id, vehicles_used) remain untouched in types.ts and api.ts
- TypeScript compiles clean, production build succeeds
</success_criteria>

<output>
After completion, create `.planning/milestones/v3.0-phases/20-ui-terminology-rename/20-01-SUMMARY.md`
</output>
