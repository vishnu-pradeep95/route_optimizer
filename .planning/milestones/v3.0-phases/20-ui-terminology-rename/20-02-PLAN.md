---
phase: 20-ui-terminology-rename
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - apps/kerala_delivery/dashboard/src/pages/UploadRoutes.tsx
  - apps/kerala_delivery/api/main.py
  - apps/kerala_delivery/driver_app/index.html
autonomous: true
requirements:
  - UI-01
  - UI-02

must_haves:
  truths:
    - "Duplicate location warnings are collapsed by default, showing a one-line summary per cluster"
    - "Expanding a duplicate warning cluster reveals addresses with driver name badges"
    - "Weight displays show clean rounded numbers (e.g., '14.2 kg' not '14.199999809265137 kg')"
    - "Distance displays show rounded numbers in both dashboard and Driver PWA"
    - "API responses return rounded weight_kg values for all stops"
    - "Driver PWA works unchanged with existing API field names (vehicle_id still present)"
  artifacts:
    - path: "apps/kerala_delivery/dashboard/src/pages/UploadRoutes.tsx"
      provides: "Redesigned DuplicateWarnings component with collapsed clusters and driver name badges"
      contains: "orderDriverMap"
    - path: "apps/kerala_delivery/api/main.py"
      provides: "API-level rounding of stop.weight_kg"
      contains: "round(stop.weight_kg"
    - path: "apps/kerala_delivery/driver_app/index.html"
      provides: "Client-side .toFixed(1) for weight and distance display"
      contains: "toFixed(1)"
  key_links:
    - from: "apps/kerala_delivery/dashboard/src/pages/UploadRoutes.tsx"
      to: "routeDetails state"
      via: "order_id -> driver lookup built from routeDetails Map"
      pattern: "orderDriverMap"
    - from: "apps/kerala_delivery/api/main.py"
      to: "stop.weight_kg"
      via: "round() in response serialization"
      pattern: "round\\(stop\\.weight_kg"
---

<objective>
Redesign duplicate location warnings with collapsible clusters showing driver names, and fix floating-point display noise across the API, dashboard, and Driver PWA.

Purpose: Office staff need cleaner warning display (15+ clusters were overwhelming) and users reported raw float values like "14.199999809265137 kg" in the Driver PWA. Fix at API level for all clients, plus client-side defense.
Output: Redesigned DuplicateWarnings component, API-level float rounding, dashboard and Driver PWA display fixes.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v3.0-phases/20-ui-terminology-rename/20-CONTEXT.md
@.planning/milestones/v3.0-phases/20-ui-terminology-rename/20-RESEARCH.md
@.planning/milestones/v3.0-phases/20-ui-terminology-rename/20-01-SUMMARY.md

@apps/kerala_delivery/dashboard/src/pages/UploadRoutes.tsx
@apps/kerala_delivery/api/main.py
@apps/kerala_delivery/driver_app/index.html

<interfaces>
<!-- Key types the executor needs. Extracted from codebase. -->

From types.ts:
```typescript
export interface DuplicateLocationWarning {
  order_ids: string[];
  addresses: string[];
  max_distance_m: number;
}

export interface RouteDetail {
  route_id: string;
  vehicle_id: string;    // Now contains driver name (Phase 19)
  driver_name: string;
  stops: RouteStop[];
  // ...
}

export interface RouteStop {
  order_id: string;
  weight_kg: number;
  distance_from_prev_km: number;
  // ...
}
```

From UploadRoutes.tsx state (available for order->driver lookup):
```typescript
const [routes, setRoutes] = useState<RouteSummary[]>([]);
const [routeDetails, setRouteDetails] = useState<Map<string, RouteDetail>>(new Map());
```

Current DuplicateWarnings component (lines 231-268 of UploadRoutes.tsx):
- Takes `{ warnings: DuplicateLocationWarning[] }` as props
- Each cluster uses DaisyUI `tw:collapse` with `defaultChecked` (expanded by default)
- Shows order IDs and addresses, no driver names

Current API float locations needing rounding (main.py):
- Line 2103: `"weight_kg": stop.weight_kg` (NOT rounded)
- Line 2184: `"weight_kg": stop.weight_kg` (NOT rounded)
- distance_from_prev_km is already rounded to 2 decimals at lines 2106, 2187

Current Driver PWA float locations (index.html):
- Line 1317-1318: `stop.distance_from_prev_km` (no rounding)
- Line 1330: `stop.weight_kg` (no rounding)
- Line 1364: `stop.weight_kg` (no rounding)
- Line 1365: `stop.distance_from_prev_km` (no rounding)
- Line 1439: `stop.weight_kg` in map popup (no rounding)

Dashboard display locations needing .toFixed() (UploadRoutes.tsx):
- Line 935: `route.total_distance_km` (no .toFixed)
- Line 937: `route.total_weight_kg` (no .toFixed)
- Line 999: `stop.weight_kg` (no .toFixed)
- Line 1001: `stop.distance_from_prev_km` (no .toFixed)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign DuplicateWarnings with collapsed clusters and driver name badges</name>
  <files>apps/kerala_delivery/dashboard/src/pages/UploadRoutes.tsx</files>
  <action>
1. Update the DuplicateWarnings component signature to accept an order-to-driver lookup map:
   ```typescript
   function DuplicateWarnings({
     warnings,
     orderDriverMap,
   }: {
     warnings: DuplicateLocationWarning[];
     orderDriverMap: Map<string, string>;
   })
   ```

2. Change each cluster collapse from expanded-by-default to collapsed-by-default:
   - Remove `defaultChecked` from the `<input type="checkbox" />` (line 247)
   - This makes all clusters start collapsed -- user clicks to expand

3. Update the collapsed summary line (collapse-title) to show:
   - Format: `"{N} orders near {first_address_truncated} -- within {distance}m of each other"`
   - Truncate the first address to ~35 characters with "..." suffix
   - Use `tw:text-sm` on the collapse title for compactness
   - Example: "8 orders near MAYYANNU ROAD, VATAKARA... -- within 0m of each other"

4. Update the expanded view (collapse-content) to show driver name badges:
   - For each order in the cluster, show: `<strong>{order_id}</strong>: {address}`
   - After the address, add a DaisyUI badge with driver name if available:
     ```tsx
     {orderDriverMap.get(id) && (
       <span className="tw:badge tw:badge-sm tw:badge-ghost tw:ml-2">
         {orderDriverMap.get(id)}
       </span>
     )}
     ```
   - If orderDriverMap is empty (no route data yet), show warnings without driver names (graceful degradation)

5. Build the orderDriverMap where DuplicateWarnings is rendered (around line 839):
   - Before the `<DuplicateWarnings>` JSX, compute the map:
     ```typescript
     const orderDriverMap = new Map<string, string>();
     routeDetails.forEach((detail) => {
       detail.stops.forEach((stop) => {
         orderDriverMap.set(stop.order_id, detail.vehicle_id);
       });
     });
     ```
   - Pass it as a prop: `<DuplicateWarnings warnings={...} orderDriverMap={orderDriverMap} />`

6. Also apply .toFixed() to the route card summary stats while editing this file:
   - Line 935: `{route.total_distance_km}` -> `{route.total_distance_km.toFixed(1)}`
   - Line 937: `{route.total_weight_kg}` -> `{route.total_weight_kg.toFixed(1)}`
   - Line 999: `{stop.weight_kg}` -> `{Number(stop.weight_kg).toFixed(1)}`
   - Line 1001: `{stop.distance_from_prev_km}` -> `{Number(stop.distance_from_prev_km).toFixed(1)}`
  </action>
  <verify>
    <automated>cd /home/vishnu/projects/routing_opt/apps/kerala_delivery/dashboard && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    DuplicateWarnings clusters are collapsed by default with summary lines showing address and count. Expanded view shows driver name badges per order. Route card stats display clean rounded numbers. TypeScript compiles clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix floating-point display at API level and Driver PWA</name>
  <files>
    apps/kerala_delivery/api/main.py,
    apps/kerala_delivery/driver_app/index.html
  </files>
  <action>
1. API-level fix in main.py -- round stop.weight_kg in ALL response builders:
   - Line 2103: Change `"weight_kg": stop.weight_kg` to `"weight_kg": round(stop.weight_kg, 1)`
   - Line 2184: Change `"weight_kg": stop.weight_kg` to `"weight_kg": round(stop.weight_kg, 1)`
   - Search for any OTHER occurrences of `"weight_kg": stop.weight_kg` in the file and round those too
   - Do NOT change `"total_weight_kg"` lines -- those already use `round()` (lines 2093, 2128, 2484, 3425)
   - Do NOT change `distance_from_prev_km` -- already rounded to 2 decimals (lines 2106, 2187)
   - This is the primary fix: ensures ALL clients (dashboard, Driver PWA, future clients) get clean data
   - IMPORTANT: Do NOT rename any API field names (vehicle_id stays vehicle_id per UI-02 requirement)

2. Driver PWA defense-in-depth fix in index.html:
   - Line 1318: `${stop.distance_from_prev_km} km away` -> `${Number(stop.distance_from_prev_km).toFixed(1)} km away`
   - Line 1330: `${stop.weight_kg} kg` -> `${Number(stop.weight_kg).toFixed(1)} kg`
   - Line 1364: `${stop.weight_kg} kg` -> `${Number(stop.weight_kg).toFixed(1)} kg`
   - Line 1365: `${stop.distance_from_prev_km} km` -> `${Number(stop.distance_from_prev_km).toFixed(1)} km`
   - Line 1439: `${stop.weight_kg} kg` -> `${Number(stop.weight_kg).toFixed(1)} kg`
   - Use `Number()` wrapper before `.toFixed()` to handle any string values safely

3. Verify no other unrounded float displays exist:
   - Search Driver PWA for other `stop.weight` or `stop.distance` references
   - Search main.py for other `stop.weight_kg` in response dicts
  </action>
  <verify>
    <automated>cd /home/vishnu/projects/routing_opt && source .venv/bin/activate && python -c "
# Verify API rounding is applied
import ast, sys
with open('apps/kerala_delivery/api/main.py') as f:
    content = f.read()
# Check that no unrounded weight_kg remains in response dicts
import re
unrounded = re.findall(r'\"weight_kg\":\s*stop\.weight_kg(?!\s*,\s*\d)', content)
if unrounded:
    print(f'FAIL: Found {len(unrounded)} unrounded weight_kg: {unrounded}')
    sys.exit(1)
rounded = re.findall(r'\"weight_kg\":\s*round\(stop\.weight_kg', content)
print(f'OK: {len(rounded)} weight_kg fields use round()')
# Check Driver PWA has toFixed
with open('apps/kerala_delivery/driver_app/index.html') as f:
    pwa = f.read()
raw_weight = re.findall(r'\$\{stop\.weight_kg\}', pwa)
if raw_weight:
    print(f'FAIL: Found {len(raw_weight)} unrounded weight_kg in PWA')
    sys.exit(1)
print('OK: All PWA weight_kg uses toFixed()')
print('ALL CHECKS PASSED')
"</automated>
  </verify>
  <done>
    API returns rounded weight_kg (1 decimal) for all stops. Driver PWA displays all weights and distances with .toFixed(1). No raw IEEE 754 floats leak through to any UI. API field names unchanged (vehicle_id preserved for PWA compatibility per UI-02).
  </done>
</task>

</tasks>

<verification>
1. `cd apps/kerala_delivery/dashboard && npm run build` -- production build succeeds
2. `source .venv/bin/activate && pytest tests/apps/ -k "route" -x --timeout=30` -- API tests pass (no field name changes)
3. Grep for unrounded weight_kg: `grep -n '"weight_kg": stop.weight_kg' apps/kerala_delivery/api/main.py` returns nothing
4. Grep for raw floats in PWA: `grep -n 'stop.weight_kg}' apps/kerala_delivery/driver_app/index.html` returns nothing (all use toFixed)
5. DuplicateWarnings component accepts orderDriverMap prop and renders collapsed clusters
</verification>

<success_criteria>
- Duplicate warnings collapsed by default with "N orders near [address]..." summary
- Expanded warnings show driver name badge per order (from routeDetails cross-reference)
- API rounds stop.weight_kg to 1 decimal in all response builders
- Dashboard route cards show .toFixed(1) for weight and distance
- Driver PWA shows .toFixed(1) for weight and distance in hero card, compact card, and map popup
- API field names remain unchanged (vehicle_id, distance_from_prev_km) -- Driver PWA works without changes to its logic
- All existing tests pass (no API contract changes)
</success_criteria>

<output>
After completion, create `.planning/milestones/v3.0-phases/20-ui-terminology-rename/20-02-SUMMARY.md`
</output>
