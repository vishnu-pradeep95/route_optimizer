---
phase: 21-playwright-e2e-test-suite
plan: 03
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - e2e/dashboard.spec.ts
  - e2e/license.spec.ts
  - docker-compose.license-test.yml
autonomous: true
requirements:
  - TEST-03
  - TEST-04
must_haves:
  truths:
    - "Dashboard renders route cards with vehicle data after CSV upload"
    - "Dashboard QR sheet link generates an HTML page with QR codes"
    - "Dashboard map page loads with a Leaflet map container"
    - "API returns 503 with license_status 'invalid' when accessed in production mode with no valid license"
    - "Health endpoint still responds even with invalid license (for diagnostics)"
  artifacts:
    - path: "e2e/dashboard.spec.ts"
      provides: "Dashboard E2E tests for route cards, QR sheet, and map"
      min_lines: 60
    - path: "e2e/license.spec.ts"
      provides: "License validation tests verifying 503 responses in production mode"
      min_lines: 40
    - path: "docker-compose.license-test.yml"
      provides: "Docker Compose override to run API in production mode with invalid license on port 8001"
      min_lines: 8
  key_links:
    - from: "e2e/dashboard.spec.ts"
      to: "e2e/helpers/setup.ts"
      via: "import for uploadTestCSV"
      pattern: "import.*setup"
    - from: "e2e/dashboard.spec.ts"
      to: "http://localhost:8000/dashboard/"
      via: "page.goto('/dashboard/')"
      pattern: "goto.*dashboard"
    - from: "e2e/license.spec.ts"
      to: "http://localhost:8001"
      via: "request against production-mode API container"
      pattern: "localhost:8001"
    - from: "docker-compose.license-test.yml"
      to: "docker-compose.yml"
      via: "extends base API service with production env override"
      pattern: "ENVIRONMENT.*production"
---

<objective>
Implement Dashboard E2E tests (route cards, QR sheet, map) and License validation E2E tests (503 responses in production mode) using a separate Docker Compose override for the license test container.

Purpose: Delivers TEST-03 (Dashboard display verification) and TEST-04 (license enforcement verification), completing all Playwright E2E coverage for Phase 21.
Output: `npx playwright test --project=dashboard` and `npx playwright test --project=license` both pass.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-playwright-e2e-test-suite/21-CONTEXT.md
@.planning/phases/21-playwright-e2e-test-suite/21-RESEARCH.md
@.planning/phases/21-playwright-e2e-test-suite/21-01-SUMMARY.md
@apps/kerala_delivery/dashboard/src/pages/UploadRoutes.tsx
@apps/kerala_delivery/api/main.py
@docker-compose.yml

<interfaces>
<!-- From Plan 01 -- executor needs these imports -->

From e2e/helpers/setup.ts (created in Plan 01):
  export async function validateApiKey(request: APIRequestContext): Promise<void>;
  export async function uploadTestCSV(request: APIRequestContext, csvPath: string): Promise<Record<string, unknown>>;
  export async function waitForHealthy(request: APIRequestContext, baseURL?: string): Promise<void>;
  export const TEST_CSV_PATH: string;

From playwright.config.ts (created in Plan 01):
  dashboard project uses viewport: { width: 1280, height: 800 }
  license project uses default viewport
  baseURL: 'http://localhost:8000'

<!-- Dashboard DOM structure (from RESEARCH.md) -->

Dashboard key selectors:
  .upload-section          -- drag-and-drop upload zone
  .drop-zone               -- file drop area
  .route-cards             -- container for vehicle route cards
  .tw\:card                -- DaisyUI card component (inside .route-cards)
  .tw\:badge.tw\:badge-neutral  -- vehicle ID badge (shows VEH-XX)
  .numeric                 -- stats spans (stops, km, min, kg)
  a.print-sheet-btn        -- QR sheet link (opens /api/qr-sheet)
  .qr-section              -- QR code display section
  .leaflet-container       -- Leaflet map container (on LiveMap page)

Dashboard is a React SPA served from Docker volume at /dashboard/
Dashboard navigation: sidebar with page links (Upload Routes, Live Map, Run History, Fleet)

<!-- License middleware response (from main.py lines 367-374) -->

503 response shape:
  { "detail": "License expired or invalid. Contact support.", "license_status": "invalid" }

Health endpoint (/health) always responds even with invalid license.
When license is invalid, /health response gets X-License-Status header.
License enforcement ONLY works when ENVIRONMENT=production (dev mode overrides to VALID).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Dashboard E2E test spec (TEST-03)</name>
  <files>e2e/dashboard.spec.ts</files>
  <action>
Create `e2e/dashboard.spec.ts` with tests for route card rendering, QR sheet generation, and map loading. Uses desktop viewport (1280x800, set by dashboard project in config).

Structure:
```
test.describe.configure({ mode: 'serial' });

test.describe('Dashboard', () => {
  test.beforeAll(async ({ request }) => {
    // 1. Validate API key
    // 2. Upload test CSV via API to ensure routes exist
    //    (use uploadTestCSV from helpers -- bypasses UI, sets up data)
  });

  // Tests follow...
});
```

**Test 1: Dashboard loads and shows route cards**
- Navigate to /dashboard/
- Wait for the page to fully load (React SPA hydration)
- The dashboard should show existing routes (loaded from API on mount)
- Assert .route-cards container is visible (timeout: 15_000 -- fetches routes + details)
- Assert at least one .tw\:card exists inside .route-cards (use CSS selector with escaped colon: `.tw\\:card`)
- Assert the first card contains a vehicle ID badge (`.tw\\:badge`)
- Assert the first card shows stats with "stops" text visible
- Assert the first card shows stats with "km" text visible

**Test 2: Route card displays vehicle and driver data**
- On the same page (routes already loaded from Test 1)
- Get the first route card
- Assert it contains a driver name (text content is not empty)
- Assert it has numeric stat values (`.numeric` spans with actual numbers, not "0" or empty)
- Assert the card has clickable elements (expand/details functionality)

**Test 3: QR sheet generation**
- Assert QR sheet link exists: look for a link/button that navigates to /api/qr-sheet or contains "QR" text
- Use Playwright `request` fixture to directly GET /api/qr-sheet (with API key header)
- Assert response status 200
- Assert response content-type contains 'text/html'
- Assert response body contains QR-related content (e.g., '<img' tags for QR images, or SVG QR codes)
- Assert response body is substantial (> 500 characters -- not an empty page)

**Test 4: Map page loads with Leaflet container**
- Navigate to the Live Map page. The Dashboard uses client-side routing -- look for a sidebar link or navigation element that says "Live Map" or has a map icon.
- If sidebar navigation exists: click the Live Map link
- If URL-based: navigate to /dashboard/ and then interact with navigation
- Wait for `.leaflet-container` to be visible (timeout: 15_000)
- Assert the Leaflet container has non-zero dimensions (width > 0 and height > 0)
- Do NOT assert tile rendering (tiles load from external CDN -- anti-pattern from RESEARCH.md)
- Assert no JavaScript errors in console related to map initialization

Important implementation notes:
- Dashboard is a React SPA -- allow time for hydration and API fetches
- Use `.tw\\:card` (double-escaped backslash in TypeScript string) for Tailwind v4 prefix selectors
- The upload-section/drop-zone may or may not be visible depending on whether routes exist -- routes SHOULD exist from beforeAll upload
- Do NOT test drag-and-drop upload UI (that's Driver PWA territory) -- Dashboard data setup is via API in beforeAll
- If the dashboard page requires clicking through the sidebar, use `getByRole('link')` or `getByText()` for navigation
  </action>
  <verify>
    <automated>cd /home/vishnu/projects/routing_opt && npx playwright test --project=dashboard 2>&1 | tail -20</automated>
  </verify>
  <done>All Dashboard tests pass. Route cards render with vehicle data and stats, QR sheet generates valid HTML, and Leaflet map container loads on the map page. Desktop viewport (1280x800) used throughout.</done>
</task>

<task type="auto">
  <name>Task 2: Create License validation E2E test spec + Docker Compose override (TEST-04)</name>
  <files>e2e/license.spec.ts, docker-compose.license-test.yml</files>
  <action>
Create two files:

**1. `docker-compose.license-test.yml`** -- Docker Compose override file that runs a second API container in production mode with an invalid license key on port 8001.

```yaml
# Override for license validation E2E tests.
# Usage: docker compose -f docker-compose.yml -f docker-compose.license-test.yml up -d api-license-test
# Starts a production-mode API on port 8001 with invalid license for 503 testing.

services:
  api-license-test:
    # Copy the build/image config from the main api service
    build:
      context: .
      dockerfile: infra/Dockerfile.api
    ports:
      - "8001:8000"
    environment:
      - ENVIRONMENT=production
      - LICENSE_KEY=invalid-test-key-for-e2e
      - DATABASE_URL=${DATABASE_URL}
      - API_KEY=${API_KEY}
      - GOOGLE_MAPS_API_KEY=not-needed-for-license-test
    depends_on:
      db:
        condition: service_healthy
    # No healthcheck needed -- we EXPECT it to return 503
```

Inspect the existing `docker-compose.yml` to get the correct Dockerfile path, build context, and DATABASE_URL reference. The api-license-test service should mirror the main api service's build config but with production environment and invalid license.

**2. `e2e/license.spec.ts`** -- License validation tests using Playwright's `request` fixture against the production-mode API at localhost:8001.

Structure:
```
test.describe('License Validation (Production Mode)', () => {
  // These tests run against a separate API container on port 8001
  // that is started in production mode with an invalid license key.

  test.beforeAll(async ({ request }) => {
    // Start the license-test container
    // Run: docker compose -f docker-compose.yml -f docker-compose.license-test.yml up -d api-license-test
    // Wait for the container to be ready (give it a few seconds to start)
    // Use a simple retry loop with request.get('http://localhost:8001/health')
    // -- /health always responds, even with invalid license
  });

  test.afterAll(async () => {
    // Stop and remove the license-test container
    // Run: docker compose -f docker-compose.yml -f docker-compose.license-test.yml stop api-license-test
    // Run: docker compose -f docker-compose.yml -f docker-compose.license-test.yml rm -f api-license-test
  });

  // Tests follow...
});
```

**Test 1: Health endpoint responds with license status header**
- GET http://localhost:8001/health (use `request.get()` with full URL, not relative)
- Expect status 200 (health is always allowed)
- Expect response header `X-License-Status` to equal `invalid`
- Expect body to have `status` field

**Test 2: API endpoints return 503 with invalid license**
- GET http://localhost:8001/api/routes
- Expect status 503
- Expect body to match: `{ detail: "License expired or invalid. Contact support.", license_status: "invalid" }` (exact match per user decision for full response validation)

**Test 3: Config endpoint returns 503**
- GET http://localhost:8001/api/config
- Expect status 503
- Verify same 503 response body shape

**Test 4: Vehicles endpoint returns 503**
- GET http://localhost:8001/api/vehicles (with X-API-Key header)
- Expect status 503

Important implementation notes:
- The license test container runs on port 8001 (NOT 8000) to avoid conflicting with the main stack
- Use `execSync` from `child_process` in beforeAll/afterAll to manage Docker containers (Playwright runs in Node.js)
- The beforeAll must wait for the container to be ready -- poll /health until it responds (may take 5-10 seconds for container startup + DB connection)
- Set a generous timeout for beforeAll (90_000ms) since container startup is slow
- The container needs DATABASE_URL to connect to the same database (or its own -- either works since license check happens before any DB queries for endpoints)
- Do NOT hardcode the baseURL for these tests -- use full URLs like `http://localhost:8001/api/routes` since the playwright.config.ts baseURL points to port 8000
- License enforcement only works in ENVIRONMENT=production per main.py line 174
- After tests complete, always clean up the container in afterAll (stop + rm)
  </action>
  <verify>
    <automated>cd /home/vishnu/projects/routing_opt && npx playwright test --project=license 2>&1 | tail -20</automated>
  </verify>
  <done>All License tests pass. Production-mode API container starts on port 8001 with invalid license, returns 503 for all endpoints except /health, and is cleaned up after tests complete. Response body validated with exact match: { detail, license_status }.</done>
</task>

</tasks>

<verification>
1. `npx playwright test --project=dashboard` passes all 4 tests (TEST-03)
2. `npx playwright test --project=license` passes all 4 tests (TEST-04)
3. `npx playwright test` (full suite -- all 4 projects) passes end-to-end
4. License test container starts and stops cleanly (no orphan containers)
5. Dashboard tests verify route cards, QR sheet, and map at desktop viewport
6. License tests verify exact 503 response shape per user decision
</verification>

<success_criteria>
- `npx playwright test --project=dashboard` passes -- route cards with vehicle data, QR sheet HTML, Leaflet map container
- `npx playwright test --project=license` passes -- 503 on all endpoints with invalid license, /health still responds with X-License-Status header
- `npx playwright test` (full suite) runs all 4 projects sequentially with zero failures
- docker-compose.license-test.yml cleanly starts/stops the production-mode API container on port 8001
</success_criteria>

<output>
After completion, create `.planning/phases/21-playwright-e2e-test-suite/21-03-SUMMARY.md`
</output>
