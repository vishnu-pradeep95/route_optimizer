---
phase: 06-dashboard-ui-overhaul
task: testing-and-bugfix
total_tasks: N/A (ad-hoc testing session)
status: in_progress
last_updated: 2026-03-03T02:57:58.902Z
---

<current_state>
Mid-session testing and bug-fixing triggered by user report: "deactivated all fleets, started new upload, vehicles still got assigned." Found and fixed two critical bugs. Thorough browser testing partially complete.
</current_state>

<completed_work>

- **Bug 1 — Stale Docker image**: The running `lpg-api` container (up 45+ hours) had old code with a `_build_fleet()` fallback that created 13 hardcoded config-based vehicles when `get_active_vehicles()` returned empty. Current code on disk correctly raises HTTP 400. **Fix**: Rebuilt container with `docker compose up -d --build api`. Container service name is `api` (not `lpg-api`).

- **Bug 2 — Browser caching stale GET responses**: `apiFetch()` in `dashboard/src/lib/api.ts` didn't set `cache: "no-store"`, so after PUT/DELETE mutations, the browser served cached GET responses. Fleet reactivation appeared broken — API updated correctly but UI showed stale state. **Fix**: Added `cache: "no-store"` to fetch call. Committed as `02c5dc5`.

- **Testing completed**:
  - Fleet Management: activate/deactivate/edit/cancel/refresh/active-only filter — all work
  - Upload & Routes: summary stats, route cards, expand/collapse, QR codes, stop list — functional
  - Run History: table loads, row expand with route detail tables — works
  - Sidebar navigation between all pages — works
</completed_work>

<remaining_work>

- **Driver App PWA testing** (`/driver` path, served via StaticFiles mount) — not tested at all
- **Live Map page** — not tested
- **Upload flow with file** — didn't test actual file upload (just viewed existing results)
- **Pre-existing visual issues on Upload & Routes page**:
  - Route card header: "VEH-O1Driver 1" — missing gap between badge and driver name
  - Route card stats: "26 stops111.31 km196 min426 kg" — `tw-flex tw-gap-4` not applying
  - Summary stats stacked vertically — `tw-stats tw-stats-horizontal` not working
  - These are DaisyUI `tw-` prefix issues, not regressions from this session
- **Docker `db-init` failure**: Container exits with IntegrityError on geocode_cache unique constraint (`address_norm, source` duplicate). Unrelated to the fixes but blocks clean `docker compose up`. The API starts fine with `docker start lpg-api` since DB is already migrated.
</remaining_work>

<decisions_made>

- Fixed the cache issue with `cache: "no-store"` on all GET requests rather than adding cache-busting query params — cleaner, works universally
- Did NOT fix the pre-existing DaisyUI/Tailwind visual issues — they're a separate concern from the reported bug and need their own investigation into why `tw-` prefixed utility classes aren't generating proper CSS
- Started API container directly with `docker start lpg-api` to bypass the failed `db-init` dependency
</decisions_made>

<blockers>
- `db-init` container fails on `geocode_cache` unique constraint violation — needs investigation but doesn't block normal operation since DB is already migrated
</blockers>

<context>
The root cause of both bugs was the Docker image being 45+ hours stale. The `_build_fleet()` fallback was legacy code from before fleet management was DB-driven. The browser cache issue existed all along but was masked when vehicles were always active (the cached response matched reality). It only became visible when the user started toggling vehicle states rapidly.

The visual issues on Upload & Routes (stats layout, card spacing) are pre-existing from the Phase 06 DaisyUI migration. The `tw-` prefix on Tailwind utility classes (`tw-flex`, `tw-gap-4`, `tw-stats-horizontal`) isn't generating proper CSS. DaisyUI component classes (`tw-badge`, `tw-card`, `tw-table`) work fine. This suggests the Tailwind v4 prefix configuration works for component classes but may have issues with some utility classes. Needs dedicated investigation.
</context>

<next_action>
Start with: Investigate why `tw-` prefixed Tailwind utility classes (tw-flex, tw-gap-4, tw-stats-horizontal) don't apply on the Upload & Routes page. Check `apps/kerala_delivery/dashboard/src/index.css` for the Tailwind import and prefix config. Then fix the 3 visual issues on route cards/summary stats. After that, test the Driver App PWA and Live Map page.
</next_action>
