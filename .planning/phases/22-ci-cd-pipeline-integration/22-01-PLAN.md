---
phase: 22-ci-cd-pipeline-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/apps/kerala_delivery/api/test_api.py
  - tests/core/database/test_database.py
  - tests/test_e2e_pipeline.py
  - tests/integration/test_osrm_vroom_pipeline.py
  - tests/scripts/test_import_orders.py
  - tests/scripts/test_geocode_batch.py
  - tests/core/geocoding/test_cache.py
  - tests/core/geocoding/test_google_adapter.py
  - tests/conftest.py
  - .github/workflows/ci.yml
autonomous: true
requirements: [CICD-01]

must_haves:
  truths:
    - "All 420+ pytest tests pass with zero failures"
    - "CI Python Tests job runs green on push to main"
    - "No xfail markers or skip decorators used to hide failures"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Updated CI workflow with correct test count comments"
      contains: "420+"
    - path: "tests/apps/kerala_delivery/api/test_api.py"
      provides: "Fixed API tests with proper async mocking"
    - path: "tests/core/database/test_database.py"
      provides: "Fixed database tests with proper async mocking"
  key_links:
    - from: "tests/**/*.py"
      to: "core/database/connection.py"
      via: "AsyncMock session override"
      pattern: "AsyncMock|mock_session|override_get_session"
    - from: ".github/workflows/ci.yml"
      to: "tests/"
      via: "pytest runner"
      pattern: "python -m pytest tests/"
---

<objective>
Fix all 64 pre-existing pytest failures so the CI Python Tests job passes green.

Purpose: CICD-01 requires the GitHub Actions pipeline to pass. Currently 64 of 426 pytest tests fail due to SQLAlchemy async/greenlet issues and inadequate mocking of database-dependent tests. These must be fixed (not skipped/xfailed) before adding new CI jobs.

Output: All 420+ pytest tests pass locally and in CI. CI YAML comments updated from "211+" to "420+".
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-ci-cd-pipeline-integration/22-CONTEXT.md
@.planning/phases/22-ci-cd-pipeline-integration/22-RESEARCH.md
@.planning/phases/21-playwright-e2e-test-suite/21-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From core/database/connection.py:
```python
# get_session is the FastAPI dependency that yields AsyncSession
async def get_session() -> AsyncGenerator[AsyncSession, None]:
    async with async_session_factory() as session:
        yield session
```

From tests/apps/kerala_delivery/api/test_api.py (existing working pattern):
```python
@pytest.fixture
def mock_session():
    session = AsyncMock()
    session.commit = AsyncMock()
    session.rollback = AsyncMock()
    session.close = AsyncMock()
    return session

@pytest.fixture
def client(mock_session):
    async def override_get_session():
        yield mock_session
    app.dependency_overrides[get_session] = override_get_session
    yield TestClient(app)
    app.dependency_overrides.clear()
```

From requirements.txt:
```
pytest==9.0.2
pytest-asyncio==1.3.0
SQLAlchemy==2.0.46
greenlet==3.3.2
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diagnose and fix all 64 pytest failures</name>
  <files>
    tests/apps/kerala_delivery/api/test_api.py,
    tests/core/database/test_database.py,
    tests/test_e2e_pipeline.py,
    tests/integration/test_osrm_vroom_pipeline.py,
    tests/scripts/test_import_orders.py,
    tests/scripts/test_geocode_batch.py,
    tests/core/geocoding/test_cache.py,
    tests/core/geocoding/test_google_adapter.py,
    tests/conftest.py
  </files>
  <action>
    Run `python3 -m pytest tests/ -q --tb=short 2>&1 | tail -80` to identify the exact 64 failing tests.

    Categorize failures into groups:
    1. **MissingGreenlet errors** -- SQLAlchemy async operations accessed in sync context. Fix by ensuring all session operations use AsyncMock with properly configured return values.
    2. **DB-dependent tests without proper mocking** -- Tests that call repository functions without mocking them. Fix by patching `core.database.repository.*` functions.
    3. **API key enforcement failures** -- Tests written before API_KEY was added. Fix by including API_KEY in test client headers or environment.
    4. **Integration tests needing running services** -- Tests that need OSRM/VROOM/PostgreSQL. Fix by adding proper mocks for external service calls (httpx, asyncpg).

    Fix strategy per category:
    - For AsyncSession mocking: Use the working pattern from test_api.py (AsyncMock with commit/rollback/close configured).
    - For repository function mocking: Patch at the module level where the function is imported (e.g., `@patch('apps.kerala_delivery.api.main.repository.save_routes')`).
    - For pytest-asyncio: If version 1.3.0 causes issues, check if `asyncio_mode = "auto"` or explicit `@pytest.mark.asyncio` markers are needed.
    - Do NOT use `@pytest.mark.xfail` or `@pytest.mark.skip` -- per user decision, all 64 must actually pass.
    - Do NOT change production code -- only fix test files and conftest.py.

    After fixing, run `python3 -m pytest tests/ -q --tb=short` and verify ALL tests pass (expected: 420+ pass, 0 fail).
  </action>
  <verify>
    <automated>python3 -m pytest tests/ -q --tb=short</automated>
  </verify>
  <done>All 420+ pytest tests pass with 0 failures and 0 errors. No xfail or skip markers added.</done>
</task>

<task type="auto">
  <name>Task 2: Update CI YAML comments and verify CI compatibility</name>
  <files>.github/workflows/ci.yml</files>
  <action>
    Update `.github/workflows/ci.yml`:
    1. Change the header comment from "211+ tests" to "420+ tests" (line ~8 area: `#   1. Test: run the full pytest suite (211+ tests)`)
    2. Verify the pytest command matches what now passes: `python -m pytest tests/ -q --tb=short`
    3. Verify environment variables are set correctly: `RATE_LIMIT_ENABLED: "false"`, `DATABASE_URL: "postgresql+asyncpg://test:test@localhost:5432/test"`, `ENVIRONMENT: "development"`
    4. Do NOT change the existing job structure, action versions, or concurrency settings.
    5. Do NOT add the E2E job yet (that's Plan 02).
  </action>
  <verify>
    <automated>grep -c "420+" .github/workflows/ci.yml</automated>
  </verify>
  <done>CI YAML comment reflects "420+ tests" instead of "211+". Pytest step configuration matches the working test suite.</done>
</task>

</tasks>

<verification>
- `python3 -m pytest tests/ -q --tb=short` exits with code 0 (all tests pass)
- `grep "420+" .github/workflows/ci.yml` returns a match
- No `xfail` or `skip` markers added to any test: `grep -r "xfail\|pytest.mark.skip" tests/ | grep -v __pycache__` returns empty
</verification>

<success_criteria>
All 420+ pytest tests pass with zero failures. CI YAML comments updated. No test markers used to hide failures.
</success_criteria>

<output>
After completion, create `.planning/phases/22-ci-cd-pipeline-integration/22-01-SUMMARY.md`
</output>
