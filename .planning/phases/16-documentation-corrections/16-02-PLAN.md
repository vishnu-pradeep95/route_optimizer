---
phase: 16-documentation-corrections
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - DEPLOY.md
autonomous: true
requirements:
  - DOCS-03
  - DOCS-04

must_haves:
  truths:
    - "DEPLOY.md references `./scripts/bootstrap.sh` for one-time setup instead of manual Docker install commands"
    - "DEPLOY.md references `./scripts/start.sh` for daily startup instead of 4-command manual sequence"
    - "DEPLOY.md has no `<REPO_URL>` placeholder -- git clone step removed entirely (project pre-installed)"
    - "DEPLOY.md has prominent Ubuntu-not-PowerShell warning before Quick Start"
    - "DEPLOY.md Sections 4 and 5 replaced with single cross-link to CSV_FORMAT.md"
    - "DEPLOY.md Quick Reference Card reflects simplified flow: Ubuntu -> start.sh -> Chrome -> upload -> print QR"
    - "DEPLOY.md daily usage section is compact (target: one printed page)"
  artifacts:
    - path: "DEPLOY.md"
      provides: "Restructured employee deployment guide with script references, Ubuntu warning, CSV cross-link"
      contains: "scripts/start.sh"
  key_links:
    - from: "DEPLOY.md"
      to: "scripts/bootstrap.sh"
      via: "One-time setup reference"
      pattern: "scripts/bootstrap\\.sh"
    - from: "DEPLOY.md"
      to: "scripts/start.sh"
      via: "Daily startup reference"
      pattern: "scripts/start\\.sh"
    - from: "DEPLOY.md"
      to: "CSV_FORMAT.md"
      via: "Cross-link replacing Sections 4 and 5"
      pattern: "CSV_FORMAT\\.md"
---

<objective>
Restructure DEPLOY.md for the non-technical office employee audience: replace manual multi-command workflows with script references, remove duplicate CDCMS content (now in CSV_FORMAT.md), add Ubuntu warning, and update the Quick Reference Card.

Purpose: DEPLOY.md is the primary guide for the office employee. It currently references manual Docker install commands, multi-step startup sequences, and duplicates CDCMS documentation that now lives in CSV_FORMAT.md. After Phase 13 (bootstrap.sh), Phase 14 (start.sh), and Phase 15 (CSV_FORMAT.md), most of this content is stale.

Output: Restructured DEPLOY.md that a non-technical employee can follow without IT support.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-documentation-corrections/16-CONTEXT.md
@.planning/phases/16-documentation-corrections/16-RESEARCH.md

@DEPLOY.md
@scripts/bootstrap.sh
@scripts/start.sh
@CSV_FORMAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure DEPLOY.md setup and daily sections</name>
  <files>DEPLOY.md</files>
  <action>
Read DEPLOY.md in full. Apply ALL changes below in a single rewrite. Use `./scripts/bootstrap.sh` and `./scripts/start.sh` (full paths, NOT `./start.sh` or `./bootstrap.sh` -- scripts live in the `scripts/` directory).

**1. Add Ubuntu warning callout** -- Insert after the intro blockquote (after line 9), before the Quick Start section:

```markdown
> **IMPORTANT:** Always use the **Ubuntu** app from the Start menu. Do NOT use
> PowerShell, Command Prompt, or Windows Terminal. All commands in this guide
> must be run in Ubuntu.
```

**2. Update Quick Start (Section 0)** -- Replace the current Quick Start block. Remove the "If Docker and Git are already installed" assumption. The project is pre-installed:

```markdown
## Quick Start (2 Steps)

```bash
# 1. Open Ubuntu from the Start menu, then:
cd ~/routing_opt
./scripts/start.sh

# 2. Open the dashboard in Chrome
#    http://localhost:8000/dashboard/
```

The start script handles everything: starts Docker, starts all services, waits
until everything is healthy, and shows you the dashboard URL.

**First time?** Follow Section 2 below for one-time setup.
```

**3. Section 2.2 (Install Software, ~lines 82-99):** Replace the entire manual Docker install block (apt-get, gpg key, repository setup) with:

```markdown
### 2.2 Install Everything

Open Ubuntu, navigate to the project folder, and run the setup script:

```bash
cd ~/routing_opt
./scripts/bootstrap.sh
```

This script:
- Installs Docker (if not already installed)
- Creates your configuration file
- Downloads Kerala map data (~300 MB, first time only)
- Starts all services

**First run takes about 15 minutes.** You'll see progress messages throughout.
If it asks you to restart Ubuntu, close and reopen Ubuntu, then run the command again.
```

**4. Section 2.3 (Download and Install, ~line 118):** Remove the `git clone <REPO_URL>` line and any surrounding instructions about cloning or USB copy. The project is pre-installed by the developer. If this section only contained the clone step, remove the entire subsection. If it has other content, keep only the non-clone parts.

**5. Section 3.1 (Start the System, ~lines 153-156):** Replace the 4-command daily startup block with:

```markdown
### 3.1 Start the System

```bash
cd ~/routing_opt
./scripts/start.sh
```

Wait for the message showing the dashboard URL. This usually takes less than a minute.
```

**6. Section 3.2 (Export from CDCMS, ~lines 167-170):** Remove the terminal `cp /mnt/c/...` file copy step. Keep only:
- Export from CDCMS instructions (the web browser part)
- Drag and drop onto the dashboard upload area
The employee should never need to use terminal commands for file transfer.

**7. Remove Sections 4 and 5 entirely** (~lines 225-298). Replace both with a single cross-link section:

```markdown
## 4. Understanding CDCMS and CSV Formats

For complete documentation on file formats, see **[CSV_FORMAT.md](CSV_FORMAT.md)**, which covers:
- What file types are accepted (.csv, .xlsx, .xls)
- Which CDCMS columns are used and which are ignored
- Standard CSV column reference
- What the system does to clean addresses
- Common error messages and how to fix them
```

**8. Renumber remaining sections** after the removal:
- Old Section 6 (Troubleshooting) becomes Section 5
- Old Section 7 (Costs) becomes Section 6
- Old Section 8 (Important Rules) becomes Section 7
- Old Section 9 (Quick Reference Card) becomes Section 8

**9. Update Table of Contents** to match new numbering:
```markdown
1. [What You Need](#1-what-you-need)
2. [One-Time Setup (New Laptop)](#2-one-time-setup-new-laptop)
3. [Daily Use — Step by Step](#3-daily-use--step-by-step)
4. [Understanding CDCMS and CSV Formats](#4-understanding-cdcms-and-csv-formats)
5. [Troubleshooting](#5-troubleshooting)
6. [Costs](#6-costs)
7. [Important Rules](#7-important-rules)
8. [Quick Reference Card](#8-quick-reference-card)
```

**10. Update Troubleshooting section** (old Section 6, now Section 5):
- "Cannot connect to Docker" fix: change to `./scripts/start.sh` (it handles Docker start)
- "System is slow" fix: change to `./scripts/start.sh` (instead of manual docker compose)
- Compress "How to update the system" subsection (Claude's discretion -- keep it brief, reference scripts)
- Do NOT change error message wording (that's Phase 17)

**11. Rewrite Quick Reference Card** (old Section 9, now Section 8):

```
┌─────────────────────────────────────────┐
│         DAILY QUICK REFERENCE           │
├─────────────────────────────────────────┤
│                                         │
│  1. OPEN Ubuntu from Start menu         │
│                                         │
│  2. START the system:                   │
│     cd ~/routing_opt                    │
│     ./scripts/start.sh                  │
│                                         │
│  3. OPEN Chrome:                        │
│     http://localhost:8000/dashboard/     │
│                                         │
│  4. UPLOAD the CDCMS file               │
│     (drag & drop onto the page)         │
│                                         │
│  5. PRINT QR codes for drivers          │
│                                         │
│  6. END OF DAY (optional):              │
│     docker compose down                 │
│                                         │
└─────────────────────────────────────────┘
```

**12. Search for broken references:** After all changes, search the entire file for:
- "Section 4" or "Section 5" references pointing to removed content
- Anchor links that no longer match (e.g., `#6-troubleshooting` should now be `#5-troubleshooting`)
- Any remaining `<REPO_URL>` placeholders
- Any remaining `install.sh` references that should be `bootstrap.sh`
Fix all found issues.
  </action>
  <verify>
    <automated>cd /home/vishnu/projects/routing_opt && grep -c '<REPO_URL>' DEPLOY.md | grep '^0$' && grep -q 'scripts/start.sh' DEPLOY.md && grep -q 'scripts/bootstrap.sh' DEPLOY.md && grep -q 'CSV_FORMAT.md' DEPLOY.md && grep -qi 'powershell' DEPLOY.md && grep -c 'Understanding the CDCMS Export' DEPLOY.md | grep '^0$' && grep -c 'What the System Does to Addresses' DEPLOY.md | grep '^0$' && echo "PASS: All DEPLOY.md restructure checks verified"</automated>
  </verify>
  <done>
- Zero `<REPO_URL>` placeholders in DEPLOY.md
- `./scripts/bootstrap.sh` referenced for one-time setup
- `./scripts/start.sh` referenced for daily startup
- Ubuntu-not-PowerShell warning present near top of document
- Old Sections 4 and 5 removed, replaced with CSV_FORMAT.md cross-link
- Table of Contents renumbered (8 sections, not 9)
- Quick Reference Card updated with simplified flow
- Troubleshooting references updated to use scripts
- No broken internal references (no "Section 4" / "Section 5" text pointing to removed content)
  </done>
</task>

</tasks>

<verification>
Run the full verification suite:

```bash
cd /home/vishnu/projects/routing_opt

# No stale placeholders or removed content
grep -c '<REPO_URL>' DEPLOY.md                          # expect: 0
grep -c 'Understanding the CDCMS Export' DEPLOY.md      # expect: 0
grep -c 'What the System Does to Addresses' DEPLOY.md   # expect: 0

# Script references present (full paths)
grep 'scripts/start.sh' DEPLOY.md                       # expect: multiple matches
grep 'scripts/bootstrap.sh' DEPLOY.md                   # expect: at least 1 match

# Cross-link to CSV_FORMAT.md present
grep 'CSV_FORMAT.md' DEPLOY.md                          # expect: at least 1 match

# Ubuntu warning present
grep -i 'powershell' DEPLOY.md                          # expect: match in warning

# No broken section references
grep -i 'section 4\|section 5' DEPLOY.md                # expect: 0 (no references to removed sections)

# Line count check (should be significantly shorter than 454)
wc -l DEPLOY.md                                         # expect: < 400 lines
```
</verification>

<success_criteria>
1. DEPLOY.md has zero `<REPO_URL>` placeholders
2. One-time setup references `./scripts/bootstrap.sh` (not manual Docker install)
3. Daily startup references `./scripts/start.sh` (not 4-command sequence)
4. Prominent Ubuntu-not-PowerShell warning visible before Quick Start
5. Old Sections 4 and 5 replaced with single CSV_FORMAT.md cross-link
6. Table of Contents has 8 sections (renumbered after removal)
7. Quick Reference Card shows: Ubuntu -> start.sh -> Chrome -> upload -> print QR
8. Troubleshooting commands reference scripts, not manual docker compose
9. No broken internal references or dangling section numbers
</success_criteria>

<output>
After completion, create `.planning/phases/16-documentation-corrections/16-02-SUMMARY.md`
</output>
